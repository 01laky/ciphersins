// Auto-generated by scripts/generate-exhaustive-tests.mjs
import { spawnSync } from "node:child_process";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { describe, expect, it } from "vitest";

const testDir = path.dirname(fileURLToPath(import.meta.url));
const rootDir = path.resolve(testDir, "../..");
const cliEntry = path.join(rootDir, "packages/ciphersins/dist/cli.js");

describe("CS-EXH CLI exhaustive expanded", () => {
	it("CS-EXH-CLI-001 jwt01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-01",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-002 jwt01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-01",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-003 jwt01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-01",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-004 jwt02 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-02",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-005 jwt02 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-02",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-006 jwt02 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-02",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-007 jwt03 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-03",
				"--format",
				"json",
				"--fail-on",
				"critical",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-008 jwt03 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-03",
				"--format",
				"pretty",
				"--fail-on",
				"critical",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-009 jwt03 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-03",
				"--format",
				"sarif",
				"--fail-on",
				"critical",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-010 jwt04 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-04",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-011 jwt04 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-04",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-012 jwt04 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-04",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-013 jwt05 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-05",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-014 jwt05 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-05",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-015 jwt05 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-05",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-016 jwt06 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-06",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-017 jwt06 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-06",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-018 jwt06 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-JWT-06",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-019 cmp01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-cmp-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-CMP-01",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-020 cmp01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-cmp-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-CMP-01",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-021 cmp01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-cmp-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-CMP-01",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-022 rng01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-01",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-023 rng01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-01",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-024 rng01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-01",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-025 rng02 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-02",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-026 rng02 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-02",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-027 rng02 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-rng-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-RNG-02",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-028 hash01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-01",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-029 hash01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-01",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-030 hash01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-01",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-031 hash02 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-02",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-032 hash02 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-02",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-033 hash02 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-02",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-034 hash03 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-03",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-035 hash03 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-03",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-036 hash03 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-03",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-037 hash04 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-04",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-038 hash04 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-04",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-039 hash04 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-04",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-040 hash05 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-05",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-041 hash05 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-05",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-042 hash05 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-hash-05/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-HASH-05",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-043 enc01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-01",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-044 enc01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-01",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-045 enc01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-01",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-046 enc02 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-02",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-047 enc02 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-02",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-048 enc02 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-02",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-049 enc03 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-03",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-050 enc03 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-03",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-051 enc03 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-03/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-03",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-052 enc04 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-04",
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-053 enc04 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-04",
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-054 enc04 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-04/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-ENC-04",
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-055 dec01 bad format json", () => {
		const dir = path.join(rootDir, "fixtures/cs-dec-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-DEC-01",
				"--format",
				"json",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-056 dec01 bad format pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-dec-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-DEC-01",
				"--format",
				"pretty",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-057 dec01 bad format sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-dec-01/bad");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--only",
				"CS-DEC-01",
				"--format",
				"sarif",
				"--fail-on",
				"medium",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(1);
	});
	it("CS-EXH-CLI-058 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-059 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-060 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-061 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-062 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-063 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-064 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-065 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-066 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-03/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-067 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-068 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-069 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-04/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-070 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-071 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-072 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-05/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-073 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-074 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-075 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-jwt-06/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-076 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-077 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-078 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-01/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-079 good good fmt json", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"json",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-080 good good fmt pretty", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"pretty",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
	it("CS-EXH-CLI-081 good good fmt sarif", () => {
		const dir = path.join(rootDir, "fixtures/cs-enc-02/good");
		const result = spawnSync(
			process.execPath,
			[
				cliEntry,
				"scan",
				dir,
				"--format",
				"sarif",
				"--fail-on",
				"high",
				"--no-color",
			],
			{
				encoding: "utf8",
				cwd: rootDir,
			},
		);
		expect(result.status).toBe(0);
	});
});
