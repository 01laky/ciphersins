// Auto-generated by scripts/generate-exhaustive-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/scan-source.js";

const cases = [
	{
		id: "HLP-001",
		label: "auth name token rng01",
		source: "export function generateToken() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-002",
		label: "auth name accessToken rng01",
		source:
			"export function generateAccessToken() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-003",
		label: "auth name refreshToken rng01",
		source:
			"export function generateRefreshToken() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-004",
		label: "auth name sessionSecret rng01",
		source:
			"export function generateSessionSecret() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-005",
		label: "auth name password rng01",
		source:
			"export function generatePassword() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-006",
		label: "auth name otp rng01",
		source: "export function generateOtp() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-007",
		label: "auth name csrfToken rng01",
		source:
			"export function generateCsrfToken() {\n  return Math.random();\n}\n",
		expect: {
			"CS-RNG-01": 1,
		},
	},
	{
		id: "HLP-008",
		label: "non-auth username rng clean",
		source: "export function pickUsername() {\n  return Math.random();\n}\n",
		expect: {},
	},
	{
		id: "HLP-009",
		label: "non-auth hashtag rng clean",
		source: "export function pickHashtag() {\n  return Math.random();\n}\n",
		expect: {},
	},
	{
		id: "HLP-010",
		label: "non-auth author rng clean",
		source: "export function pickAuthor() {\n  return Math.random();\n}\n",
		expect: {},
	},
	{
		id: "HLP-011",
		label: "non-auth publisher rng clean",
		source: "export function pickPublisher() {\n  return Math.random();\n}\n",
		expect: {},
	},
	{
		id: "HLP-012",
		label: "non-auth color rng clean",
		source: "export function pickColor() {\n  return Math.random();\n}\n",
		expect: {},
	},
	{
		id: "HLP-013",
		label: "password ctx hashPassword md5",
		source:
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		expect: {
			"CS-HASH-01": 1,
		},
	},
	{
		id: "HLP-014",
		label: "password ctx verifyPassword md5",
		source:
			'import { createHash } from "crypto";\nexport function verifyPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		expect: {
			"CS-HASH-01": 1,
		},
	},
	{
		id: "HLP-015",
		label: "password ctx deriveKeyFromPassword md5",
		source:
			'import { createHash } from "crypto";\nexport function deriveKeyFromPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		expect: {
			"CS-HASH-01": 1,
		},
	},
	{
		id: "HLP-016",
		label: "password ctx checkPassphrase md5",
		source:
			'import { createHash } from "crypto";\nexport function checkPassphrase(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		expect: {
			"CS-HASH-01": 1,
		},
	},
];

describe("CS-EXH HLP-SCAN exhaustive edge cases", () => {
	it.each(cases)("$id $label", async (c) => {
		const result = await scanSource(`case-${c.id}.ts`, c.source);
		const expected = c.expect;
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
