// Auto-generated by scripts/generate-exhaustive-tests.mjs
import ts from "typescript";
import { describe, expect, it } from "vitest";
import {
	isAuthMaterialName,
	splitCamelCase,
} from "../../packages/ciphersins/src/rules/helpers/auth-material-names.js";
import { isWeakHashAlgorithmLiteral } from "../../packages/ciphersins/src/rules/helpers/weak-hash-algorithms.js";
import { isWeakCipherAlgorithmLiteral } from "../../packages/ciphersins/src/rules/helpers/weak-cipher-algorithms.js";
import { isEcbCipherAlgorithmLiteral } from "../../packages/ciphersins/src/rules/helpers/ecb-cipher-algorithms.js";
import { isWeakBcryptCostLiteral } from "../../packages/ciphersins/src/rules/helpers/bcrypt-cost.js";
import { expressionIsLowPbkdf2IterationCount } from "../../packages/ciphersins/src/rules/helpers/pbkdf2-iterations.js";

function parseExpr(source: string): ts.CallExpression {
	const file = ts.createSourceFile(
		"t.ts",
		`${source};\n`,
		ts.ScriptTarget.Latest,
		true,
	);
	const stmt = file.statements[0];
	if (
		!stmt ||
		!ts.isExpressionStatement(stmt) ||
		!ts.isCallExpression(stmt.expression)
	)
		throw new Error("expected call");
	return stmt.expression;
}

describe("CS-EXH helper unit exhaustive", () => {
	it("CS-EXH-HLP-001 auth word token", () => {
		expect(isAuthMaterialName("token")).toBe(true);
	});
	it("CS-EXH-HLP-002 auth word jwt", () => {
		expect(isAuthMaterialName("jwt")).toBe(true);
	});
	it("CS-EXH-HLP-003 auth word session", () => {
		expect(isAuthMaterialName("session")).toBe(true);
	});
	it("CS-EXH-HLP-004 auth word sessionid", () => {
		expect(isAuthMaterialName("sessionid")).toBe(true);
	});
	it("CS-EXH-HLP-005 auth word accesstoken", () => {
		expect(isAuthMaterialName("accesstoken")).toBe(true);
	});
	it("CS-EXH-HLP-006 auth word refreshtoken", () => {
		expect(isAuthMaterialName("refreshtoken")).toBe(true);
	});
	it("CS-EXH-HLP-007 auth word bearertoken", () => {
		expect(isAuthMaterialName("bearertoken")).toBe(true);
	});
	it("CS-EXH-HLP-008 auth word idtoken", () => {
		expect(isAuthMaterialName("idtoken")).toBe(true);
	});
	it("CS-EXH-HLP-009 auth word secret", () => {
		expect(isAuthMaterialName("secret")).toBe(true);
	});
	it("CS-EXH-HLP-010 auth word password", () => {
		expect(isAuthMaterialName("password")).toBe(true);
	});
	it("CS-EXH-HLP-011 auth word passwd", () => {
		expect(isAuthMaterialName("passwd")).toBe(true);
	});
	it("CS-EXH-HLP-012 auth word pwd", () => {
		expect(isAuthMaterialName("pwd")).toBe(true);
	});
	it("CS-EXH-HLP-013 auth word passphrase", () => {
		expect(isAuthMaterialName("passphrase")).toBe(true);
	});
	it("CS-EXH-HLP-014 auth word apikey", () => {
		expect(isAuthMaterialName("apikey")).toBe(true);
	});
	it("CS-EXH-HLP-015 auth word clientsecret", () => {
		expect(isAuthMaterialName("clientsecret")).toBe(true);
	});
	it("CS-EXH-HLP-016 auth word hash", () => {
		expect(isAuthMaterialName("hash")).toBe(true);
	});
	it("CS-EXH-HLP-017 auth word otp", () => {
		expect(isAuthMaterialName("otp")).toBe(true);
	});
	it("CS-EXH-HLP-018 auth word totp", () => {
		expect(isAuthMaterialName("totp")).toBe(true);
	});
	it("CS-EXH-HLP-019 auth word pin", () => {
		expect(isAuthMaterialName("pin")).toBe(true);
	});
	it("CS-EXH-HLP-020 auth word nonce", () => {
		expect(isAuthMaterialName("nonce")).toBe(true);
	});
	it("CS-EXH-HLP-021 auth word salt", () => {
		expect(isAuthMaterialName("salt")).toBe(true);
	});
	it("CS-EXH-HLP-022 auth word hmac", () => {
		expect(isAuthMaterialName("hmac")).toBe(true);
	});
	it("CS-EXH-HLP-023 auth word digest", () => {
		expect(isAuthMaterialName("digest")).toBe(true);
	});
	it("CS-EXH-HLP-024 auth word signature", () => {
		expect(isAuthMaterialName("signature")).toBe(true);
	});
	it("CS-EXH-HLP-025 auth word checksum", () => {
		expect(isAuthMaterialName("checksum")).toBe(true);
	});
	it("CS-EXH-HLP-026 auth word csrf", () => {
		expect(isAuthMaterialName("csrf")).toBe(true);
	});
	it("CS-EXH-HLP-027 auth word authorization", () => {
		expect(isAuthMaterialName("authorization")).toBe(true);
	});
	it("CS-EXH-HLP-028 auth word credential", () => {
		expect(isAuthMaterialName("credential")).toBe(true);
	});
	it("CS-EXH-HLP-029 auth word bearer", () => {
		expect(isAuthMaterialName("bearer")).toBe(true);
	});
	it("CS-EXH-HLP-030 camel auth accessToken", () => {
		expect(isAuthMaterialName("accessToken")).toBe(true);
	});
	it("CS-EXH-HLP-031 camel auth refreshToken", () => {
		expect(isAuthMaterialName("refreshToken")).toBe(true);
	});
	it("CS-EXH-HLP-032 camel auth sessionSecret", () => {
		expect(isAuthMaterialName("sessionSecret")).toBe(true);
	});
	it("CS-EXH-HLP-033 camel auth clientSecret", () => {
		expect(isAuthMaterialName("clientSecret")).toBe(true);
	});
	it("CS-EXH-HLP-034 camel auth bearerToken", () => {
		expect(isAuthMaterialName("bearerToken")).toBe(true);
	});
	it("CS-EXH-HLP-035 camel auth idToken", () => {
		expect(isAuthMaterialName("idToken")).toBe(true);
	});
	it("CS-EXH-HLP-036 camel auth totpCode", () => {
		expect(isAuthMaterialName("totpCode")).toBe(true);
	});
	it("CS-EXH-HLP-037 camel auth csrfToken", () => {
		expect(isAuthMaterialName("csrfToken")).toBe(true);
	});
	it("CS-EXH-HLP-038 camel auth passwordHash", () => {
		expect(isAuthMaterialName("passwordHash")).toBe(true);
	});
	it("CS-EXH-HLP-039 non-auth username", () => {
		expect(isAuthMaterialName("username")).toBe(false);
	});
	it("CS-EXH-HLP-040 non-auth hashtag", () => {
		expect(isAuthMaterialName("hashtag")).toBe(false);
	});
	it("CS-EXH-HLP-041 non-auth author", () => {
		expect(isAuthMaterialName("author")).toBe(false);
	});
	it("CS-EXH-HLP-042 non-auth color", () => {
		expect(isAuthMaterialName("color")).toBe(false);
	});
	it("CS-EXH-HLP-043 non-auth publisher", () => {
		expect(isAuthMaterialName("publisher")).toBe(false);
	});
	it("CS-EXH-HLP-044 non-auth label", () => {
		expect(isAuthMaterialName("label")).toBe(false);
	});
	it("CS-EXH-HLP-045 non-auth title", () => {
		expect(isAuthMaterialName("title")).toBe(false);
	});
	it("CS-EXH-HLP-046 non-auth index", () => {
		expect(isAuthMaterialName("index")).toBe(false);
	});
	it("CS-EXH-HLP-047 non-auth count", () => {
		expect(isAuthMaterialName("count")).toBe(false);
	});
	it("CS-EXH-HLP-048 non-auth offset", () => {
		expect(isAuthMaterialName("offset")).toBe(false);
	});
	it("CS-EXH-HLP-049 non-auth width", () => {
		expect(isAuthMaterialName("width")).toBe(false);
	});
	it("CS-EXH-HLP-050 non-auth height", () => {
		expect(isAuthMaterialName("height")).toBe(false);
	});
	it("CS-EXH-HLP-051 non-auth version", () => {
		expect(isAuthMaterialName("version")).toBe(false);
	});
	it("CS-EXH-HLP-052 non-auth locale", () => {
		expect(isAuthMaterialName("locale")).toBe(false);
	});
	it("CS-EXH-HLP-053 non-auth currency", () => {
		expect(isAuthMaterialName("currency")).toBe(false);
	});
	it("CS-EXH-HLP-054 non-auth timezone", () => {
		expect(isAuthMaterialName("timezone")).toBe(false);
	});
	it("CS-EXH-HLP-055 non-auth category", () => {
		expect(isAuthMaterialName("category")).toBe(false);
	});
	it("CS-EXH-HLP-056 non-auth description", () => {
		expect(isAuthMaterialName("description")).toBe(false);
	});
	it("CS-EXH-HLP-057 non-auth comment", () => {
		expect(isAuthMaterialName("comment")).toBe(false);
	});
	it("CS-EXH-HLP-058 non-auth api_key", () => {
		expect(isAuthMaterialName("api_key")).toBe(false);
	});
	it("CS-EXH-HLP-059 non-auth apiKey", () => {
		expect(isAuthMaterialName("apiKey")).toBe(false);
	});
	it("CS-EXH-HLP-060 weak md5", () => {
		const e = parseExpr('createHash("md5")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-061 weak md4", () => {
		const e = parseExpr('createHash("md4")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-062 weak sha1", () => {
		const e = parseExpr('createHash("sha1")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-063 weak sha-1", () => {
		const e = parseExpr('createHash("sha-1")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-064 weak md2", () => {
		const e = parseExpr('createHash("md2")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-065 weak ripemd160", () => {
		const e = parseExpr('createHash("ripemd160")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-066 weak ripemd-160", () => {
		const e = parseExpr('createHash("ripemd-160")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-067 strong sha256", () => {
		const e = parseExpr('createHash("sha256")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(false);
	});
	it("CS-EXH-HLP-068 strong sha512", () => {
		const e = parseExpr('createHash("sha512")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(false);
	});
	it("CS-EXH-HLP-069 strong sha384", () => {
		const e = parseExpr('createHash("sha384")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(false);
	});
	it("CS-EXH-HLP-070 strong sha3-256", () => {
		const e = parseExpr('createHash("sha3-256")');
		expect(isWeakHashAlgorithmLiteral(e.arguments[0])).toBe(false);
	});
	it("CS-EXH-HLP-071 weak cipher des-cbc", () => {
		const e = parseExpr('createCipheriv("des-cbc", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-072 weak cipher des", () => {
		const e = parseExpr('createCipheriv("des", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-073 weak cipher des-ede3", () => {
		const e = parseExpr('createCipheriv("des-ede3", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-074 weak cipher rc4", () => {
		const e = parseExpr('createCipheriv("rc4", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-075 weak cipher rc2", () => {
		const e = parseExpr('createCipheriv("rc2", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-076 weak cipher bf", () => {
		const e = parseExpr('createCipheriv("bf", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-077 weak cipher cast", () => {
		const e = parseExpr('createCipheriv("cast", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-078 weak cipher cast5", () => {
		const e = parseExpr('createCipheriv("cast5", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-079 weak cipher rc4-arcfour", () => {
		const e = parseExpr('createCipheriv("rc4-arcfour", k, iv)');
		expect(isWeakCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-080 ecb aes-128-ecb", () => {
		const e = parseExpr('createCipheriv("aes-128-ecb", k, iv)');
		expect(isEcbCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-081 ecb aes-256-ecb", () => {
		const e = parseExpr('createCipheriv("aes-256-ecb", k, iv)');
		expect(isEcbCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-082 ecb AES-128-ECB", () => {
		const e = parseExpr('createCipheriv("AES-128-ECB", k, iv)');
		expect(isEcbCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-083 ecb aes-192-ecb", () => {
		const e = parseExpr('createCipheriv("aes-192-ecb", k, iv)');
		expect(isEcbCipherAlgorithmLiteral(e.arguments[0])).toBe(true);
	});
	it("CS-EXH-HLP-084 bcrypt cost 4", () => {
		const e = parseExpr("bcrypt.hashSync(password, 4)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-085 bcrypt cost 5", () => {
		const e = parseExpr("bcrypt.hashSync(password, 5)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-086 bcrypt cost 6", () => {
		const e = parseExpr("bcrypt.hashSync(password, 6)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-087 bcrypt cost 7", () => {
		const e = parseExpr("bcrypt.hashSync(password, 7)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-088 bcrypt cost 8", () => {
		const e = parseExpr("bcrypt.hashSync(password, 8)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-089 bcrypt cost 9", () => {
		const e = parseExpr("bcrypt.hashSync(password, 9)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(true);
	});
	it("CS-EXH-HLP-090 bcrypt cost 10", () => {
		const e = parseExpr("bcrypt.hashSync(password, 10)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-091 bcrypt cost 11", () => {
		const e = parseExpr("bcrypt.hashSync(password, 11)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-092 bcrypt cost 12", () => {
		const e = parseExpr("bcrypt.hashSync(password, 12)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-093 bcrypt cost 13", () => {
		const e = parseExpr("bcrypt.hashSync(password, 13)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-094 bcrypt cost 14", () => {
		const e = parseExpr("bcrypt.hashSync(password, 14)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-095 bcrypt cost 15", () => {
		const e = parseExpr("bcrypt.hashSync(password, 15)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-096 bcrypt cost 16", () => {
		const e = parseExpr("bcrypt.hashSync(password, 16)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-097 bcrypt cost 17", () => {
		const e = parseExpr("bcrypt.hashSync(password, 17)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-098 bcrypt cost 18", () => {
		const e = parseExpr("bcrypt.hashSync(password, 18)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-099 bcrypt cost 19", () => {
		const e = parseExpr("bcrypt.hashSync(password, 19)");
		expect(isWeakBcryptCostLiteral(e.arguments[1])).toBe(false);
	});
	it("CS-EXH-HLP-100 pbkdf2 0", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 0, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-101 pbkdf2 1", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 1, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-102 pbkdf2 999", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 999, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-103 pbkdf2 1000", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 1000, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-104 pbkdf2 50000", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 50000, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-105 pbkdf2 99999", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 99999, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(true);
	});
	it("CS-EXH-HLP-106 pbkdf2 100000", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 100000, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(false);
	});
	it("CS-EXH-HLP-107 pbkdf2 500000", () => {
		const e = parseExpr('pbkdf2Sync(p, s, 500000, 32, "sha256")');
		expect(expressionIsLowPbkdf2IterationCount(e.arguments[2])).toBe(false);
	});
	it("CS-EXH-HLP-108 split accessToken", () => {
		expect(splitCamelCase("accessToken")).toEqual(["access", "token"]);
	});
	it("CS-EXH-HLP-109 split sessionSecret", () => {
		expect(splitCamelCase("sessionSecret")).toEqual(["session", "secret"]);
	});
	it("CS-EXH-HLP-110 split API_KEY", () => {
		expect(splitCamelCase("API_KEY")).toEqual(["api", "key"]);
	});
	it("CS-EXH-HLP-111 split refreshToken", () => {
		expect(splitCamelCase("refreshToken")).toEqual(["refresh", "token"]);
	});
	it("CS-EXH-HLP-112 split hashPassword", () => {
		expect(splitCamelCase("hashPassword")).toEqual(["hash", "password"]);
	});
	it("CS-EXH-HLP-113 rng02 scan 1", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(1); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(1);
	});
	it("CS-EXH-HLP-114 rng02 scan 4", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(4); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(1);
	});
	it("CS-EXH-HLP-115 rng02 scan 8", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(8); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(1);
	});
	it("CS-EXH-HLP-116 rng02 scan 15", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(15); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(1);
	});
	it("CS-EXH-HLP-117 rng02 scan 16", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(16); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(0);
	});
	it("CS-EXH-HLP-118 rng02 scan 17", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(17); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(0);
	});
	it("CS-EXH-HLP-119 rng02 scan 32", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(32); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(0);
	});
	it("CS-EXH-HLP-120 rng02 scan 64", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(64); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(0);
	});
	it("CS-EXH-HLP-121 rng02 scan 128", async () => {
		const { scanSource, countRule } = await import("../helpers/scan-source.js");
		const r = await scanSource(
			"x.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() { return randomBytes(128); }\n',
		);
		expect(countRule(r.findings, "CS-RNG-02")).toBe(0);
	});
});
