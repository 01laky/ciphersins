// Auto-generated by scripts/generate-exhaustive-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/scan-source.js";

const cases = [
	{
		id: "001",
		label: 'JWT-01 decode-only import jwt from "jso',
		source:
			'import jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "002",
		label: "JWT-01 decode-only import * as jwt from",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "003",
		label: "JWT-01 decode-only import { decode, ver",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "004",
		label: "JWT-01 decode-only const jwt = require(",
		source:
			'const jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "005",
		label: "JWT-01 decode-only const jwt = require(",
		source:
			'const jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {},
	},
	{
		id: "006",
		label: "JWT-01 verify in inner fn suppresses decode",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "007",
		label: "JWT-01 verify in outer only flags decode",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}",
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "008",
		label: "JWT-01 direct helper verify suppresses",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "009",
		label: "JWT-01 re-export verify suppresses decode",
		source:
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "010",
		label: "JWT-02 jwt.verify(t, s)",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "011",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "012",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "013",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "014",
		label: "JWT-02 jwt.verify(t, s, { clockTolera",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "015",
		label: "JWT-02 jwt.verify(t, s, cb)",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "016",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "017",
		label: "JWT-02 jwt.verify(t, s)",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "018",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "019",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "020",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "021",
		label: "JWT-02 jwt.verify(t, s, { clockTolera",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "022",
		label: "JWT-02 jwt.verify(t, s, cb)",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "023",
		label: "JWT-02 jwt.verify(t, s, { algorithms:",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "024",
		label: "JWT-02 verify(t, s)",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "025",
		label: "JWT-02 verify(t, s, { algorithms: ['H",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "026",
		label: "JWT-02 verify(t, s, { algorithms: ['R",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "027",
		label: "JWT-02 verify(t, s, { algorithms: ['H",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "028",
		label: "JWT-02 verify(t, s, { clockTolerance:",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "029",
		label: "JWT-02 verify(t, s, cb)",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "030",
		label: "JWT-02 verify(t, s, { algorithms: ['H",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "031",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none'] ",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "032",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256',",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "033",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: '",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "034",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256']",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "035",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "036",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "037",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256']",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n",
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "038",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s)",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "039",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "040",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "041",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "042",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb)",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "043",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s)",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "044",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "045",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "046",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "047",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb)",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "048",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "049",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp:",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "050",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 }, ",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n",
		expect: {},
	},
	{
		id: "051",
		label: "JWT-01 destructured decode not tracked",
		source:
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		expect: {},
	},
	{
		id: "052",
		label: "JWT-01 verify-only clean",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		expect: {},
	},
	{
		id: "053",
		label: "JWT-01 arrow decode",
		source:
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "054",
		label: "JWT-01 async decode",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "055",
		label: "JWT-01 class method decode",
		source:
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "056",
		label: "JWT-01 IIFE decode",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "057",
		label: "JWT-02 spread algorithms not explicit",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}",
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "058",
		label: "JWT-02 empty algorithms array",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "059",
		label: "JWT-05 two-arg sign secret variant",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "060",
		label: "JWT-05 expiresIn clean variant",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "061",
		label: "JWT-05 two-arg sign secret variant",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "062",
		label: "JWT-05 expiresIn clean variant",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "063",
		label: "grid decode authenticate",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "064",
		label: "grid verify-no-alg authenticate",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "065",
		label: "grid verify-alg authenticate",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "066",
		label: "grid sign-no-exp authenticate",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "067",
		label: "grid decode handleAuth",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "068",
		label: "grid verify-no-alg handleAuth",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "069",
		label: "grid verify-alg handleAuth",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "070",
		label: "grid sign-no-exp handleAuth",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "071",
		label: "grid decode verifyRequest",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "072",
		label: "grid verify-no-alg verifyRequest",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "073",
		label: "grid verify-alg verifyRequest",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "074",
		label: "grid sign-no-exp verifyRequest",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "075",
		label: "grid decode issueCredentials",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "076",
		label: "grid verify-no-alg issueCredentials",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "077",
		label: "grid verify-alg issueCredentials",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "078",
		label: "grid sign-no-exp issueCredentials",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "079",
		label: "grid decode parseBearer",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "080",
		label: "grid verify-no-alg parseBearer",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "081",
		label: "grid verify-alg parseBearer",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "082",
		label: "grid sign-no-exp parseBearer",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "083",
		label: "grid decode validateSession",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "084",
		label: "grid verify-no-alg validateSession",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "085",
		label: "grid verify-alg validateSession",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "086",
		label: "grid sign-no-exp validateSession",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "087",
		label: "grid decode loginUser",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "088",
		label: "grid verify-no-alg loginUser",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "089",
		label: "grid verify-alg loginUser",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "090",
		label: "grid sign-no-exp loginUser",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "091",
		label: "grid decode middleware",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "092",
		label: "grid verify-no-alg middleware",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "093",
		label: "grid verify-alg middleware",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "094",
		label: "grid sign-no-exp middleware",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "095",
		label: "grid decode authenticate",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "096",
		label: "grid verify-no-alg authenticate",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "097",
		label: "grid verify-alg authenticate",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "098",
		label: "grid sign-no-exp authenticate",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "099",
		label: "grid decode handleAuth",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "100",
		label: "grid verify-no-alg handleAuth",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "101",
		label: "grid verify-alg handleAuth",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "102",
		label: "grid sign-no-exp handleAuth",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "103",
		label: "grid decode verifyRequest",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "104",
		label: "grid verify-no-alg verifyRequest",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "105",
		label: "grid verify-alg verifyRequest",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "106",
		label: "grid sign-no-exp verifyRequest",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "107",
		label: "grid decode issueCredentials",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "108",
		label: "grid verify-no-alg issueCredentials",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "109",
		label: "grid verify-alg issueCredentials",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "110",
		label: "grid sign-no-exp issueCredentials",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "111",
		label: "grid decode parseBearer",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "112",
		label: "grid verify-no-alg parseBearer",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "113",
		label: "grid verify-alg parseBearer",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "114",
		label: "grid sign-no-exp parseBearer",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "115",
		label: "grid decode validateSession",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "116",
		label: "grid verify-no-alg validateSession",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "117",
		label: "grid verify-alg validateSession",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "118",
		label: "grid sign-no-exp validateSession",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "119",
		label: "grid decode loginUser",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "120",
		label: "grid verify-no-alg loginUser",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "121",
		label: "grid verify-alg loginUser",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "122",
		label: "grid sign-no-exp loginUser",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "123",
		label: "grid decode middleware",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "124",
		label: "grid verify-no-alg middleware",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "125",
		label: "grid verify-alg middleware",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "126",
		label: "grid sign-no-exp middleware",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "127",
		label: "grid decode authenticate",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "128",
		label: "grid verify-no-alg authenticate",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "129",
		label: "grid verify-alg authenticate",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "130",
		label: "grid sign-no-exp authenticate",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "131",
		label: "grid decode handleAuth",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "132",
		label: "grid verify-no-alg handleAuth",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "133",
		label: "grid verify-alg handleAuth",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "134",
		label: "grid sign-no-exp handleAuth",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "135",
		label: "grid decode verifyRequest",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "136",
		label: "grid verify-no-alg verifyRequest",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "137",
		label: "grid verify-alg verifyRequest",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "138",
		label: "grid sign-no-exp verifyRequest",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "139",
		label: "grid decode issueCredentials",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "140",
		label: "grid verify-no-alg issueCredentials",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "141",
		label: "grid verify-alg issueCredentials",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "142",
		label: "grid sign-no-exp issueCredentials",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "143",
		label: "grid decode parseBearer",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "144",
		label: "grid verify-no-alg parseBearer",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "145",
		label: "grid verify-alg parseBearer",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "146",
		label: "grid sign-no-exp parseBearer",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "147",
		label: "grid decode validateSession",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "148",
		label: "grid verify-no-alg validateSession",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "149",
		label: "grid verify-alg validateSession",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "150",
		label: "grid sign-no-exp validateSession",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "151",
		label: "grid decode loginUser",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "152",
		label: "grid verify-no-alg loginUser",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "153",
		label: "grid verify-alg loginUser",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "154",
		label: "grid sign-no-exp loginUser",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "155",
		label: "grid decode middleware",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "156",
		label: "grid verify-no-alg middleware",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "157",
		label: "grid verify-alg middleware",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "158",
		label: "grid sign-no-exp middleware",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "159",
		label: "grid decode authenticate",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "160",
		label: "grid verify-no-alg authenticate",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "161",
		label: "grid verify-alg authenticate",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "162",
		label: "grid sign-no-exp authenticate",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "163",
		label: "grid decode handleAuth",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "164",
		label: "grid verify-no-alg handleAuth",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "165",
		label: "grid verify-alg handleAuth",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "166",
		label: "grid sign-no-exp handleAuth",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "167",
		label: "grid decode verifyRequest",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "168",
		label: "grid verify-no-alg verifyRequest",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "169",
		label: "grid verify-alg verifyRequest",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "170",
		label: "grid sign-no-exp verifyRequest",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "171",
		label: "grid decode issueCredentials",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "172",
		label: "grid verify-no-alg issueCredentials",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "173",
		label: "grid verify-alg issueCredentials",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "174",
		label: "grid sign-no-exp issueCredentials",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "175",
		label: "grid decode parseBearer",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "176",
		label: "grid verify-no-alg parseBearer",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "177",
		label: "grid verify-alg parseBearer",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "178",
		label: "grid sign-no-exp parseBearer",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "179",
		label: "grid decode validateSession",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "180",
		label: "grid verify-no-alg validateSession",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "181",
		label: "grid verify-alg validateSession",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "182",
		label: "grid sign-no-exp validateSession",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "183",
		label: "grid decode loginUser",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "184",
		label: "grid verify-no-alg loginUser",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "185",
		label: "grid verify-alg loginUser",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "186",
		label: "grid sign-no-exp loginUser",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "187",
		label: "grid decode middleware",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "188",
		label: "grid verify-no-alg middleware",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "189",
		label: "grid verify-alg middleware",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "190",
		label: "grid sign-no-exp middleware",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "191",
		label: "ext jwt03 none authenticate",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "192",
		label: "ext jwt04 ignoreExp authenticate",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "193",
		label: "ext jwt06 noTimestamp authenticate",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "194",
		label: "ext jwt02 spread alg authenticate",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "195",
		label: "ext jwt03 none handleAuth",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "196",
		label: "ext jwt04 ignoreExp handleAuth",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "197",
		label: "ext jwt06 noTimestamp handleAuth",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "198",
		label: "ext jwt02 spread alg handleAuth",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "199",
		label: "ext jwt03 none verifyRequest",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "200",
		label: "ext jwt04 ignoreExp verifyRequest",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "201",
		label: "ext jwt06 noTimestamp verifyRequest",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "202",
		label: "ext jwt02 spread alg verifyRequest",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "203",
		label: "ext jwt03 none issueCredentials",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "204",
		label: "ext jwt04 ignoreExp issueCredentials",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "205",
		label: "ext jwt06 noTimestamp issueCredentials",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "206",
		label: "ext jwt02 spread alg issueCredentials",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "207",
		label: "ext jwt03 none parseBearer",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "208",
		label: "ext jwt04 ignoreExp parseBearer",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "209",
		label: "ext jwt06 noTimestamp parseBearer",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "210",
		label: "ext jwt02 spread alg parseBearer",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "211",
		label: "ext jwt03 none validateSession",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "212",
		label: "ext jwt04 ignoreExp validateSession",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "213",
		label: "ext jwt06 noTimestamp validateSession",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "214",
		label: "ext jwt02 spread alg validateSession",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "215",
		label: "ext jwt03 none loginUser",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "216",
		label: "ext jwt04 ignoreExp loginUser",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "217",
		label: "ext jwt06 noTimestamp loginUser",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "218",
		label: "ext jwt02 spread alg loginUser",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "219",
		label: "ext jwt03 none middleware",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "220",
		label: "ext jwt04 ignoreExp middleware",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "221",
		label: "ext jwt06 noTimestamp middleware",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "222",
		label: "ext jwt02 spread alg middleware",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "223",
		label: "ext jwt03 none refreshToken",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "224",
		label: "ext jwt04 ignoreExp refreshToken",
		source:
			'import jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "225",
		label: "ext jwt06 noTimestamp refreshToken",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "226",
		label: "ext jwt02 spread alg refreshToken",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "227",
		label: "ext jwt03 none decodeClaims",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "228",
		label: "ext jwt04 ignoreExp decodeClaims",
		source:
			'import jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "229",
		label: "ext jwt06 noTimestamp decodeClaims",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "230",
		label: "ext jwt02 spread alg decodeClaims",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "231",
		label: "ext jwt03 none authenticate",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "232",
		label: "ext jwt04 ignoreExp authenticate",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "233",
		label: "ext jwt06 noTimestamp authenticate",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "234",
		label: "ext jwt02 spread alg authenticate",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "235",
		label: "ext jwt03 none handleAuth",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "236",
		label: "ext jwt04 ignoreExp handleAuth",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "237",
		label: "ext jwt06 noTimestamp handleAuth",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "238",
		label: "ext jwt02 spread alg handleAuth",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "239",
		label: "ext jwt03 none verifyRequest",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "240",
		label: "ext jwt04 ignoreExp verifyRequest",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "241",
		label: "ext jwt06 noTimestamp verifyRequest",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "242",
		label: "ext jwt02 spread alg verifyRequest",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "243",
		label: "ext jwt03 none issueCredentials",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "244",
		label: "ext jwt04 ignoreExp issueCredentials",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "245",
		label: "ext jwt06 noTimestamp issueCredentials",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "246",
		label: "ext jwt02 spread alg issueCredentials",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "247",
		label: "ext jwt03 none parseBearer",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "248",
		label: "ext jwt04 ignoreExp parseBearer",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "249",
		label: "ext jwt06 noTimestamp parseBearer",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "250",
		label: "ext jwt02 spread alg parseBearer",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "251",
		label: "ext jwt03 none validateSession",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "252",
		label: "ext jwt04 ignoreExp validateSession",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "253",
		label: "ext jwt06 noTimestamp validateSession",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "254",
		label: "ext jwt02 spread alg validateSession",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "255",
		label: "ext jwt03 none loginUser",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "256",
		label: "ext jwt04 ignoreExp loginUser",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "257",
		label: "ext jwt06 noTimestamp loginUser",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "258",
		label: "ext jwt02 spread alg loginUser",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "259",
		label: "ext jwt03 none middleware",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "260",
		label: "ext jwt04 ignoreExp middleware",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "261",
		label: "ext jwt06 noTimestamp middleware",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "262",
		label: "ext jwt02 spread alg middleware",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "263",
		label: "ext jwt03 none refreshToken",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "264",
		label: "ext jwt04 ignoreExp refreshToken",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "265",
		label: "ext jwt06 noTimestamp refreshToken",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "266",
		label: "ext jwt02 spread alg refreshToken",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "267",
		label: "ext jwt03 none decodeClaims",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "268",
		label: "ext jwt04 ignoreExp decodeClaims",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "269",
		label: "ext jwt06 noTimestamp decodeClaims",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "270",
		label: "ext jwt02 spread alg decodeClaims",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "271",
		label: "ext jwt03 none authenticate",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "272",
		label: "ext jwt04 ignoreExp authenticate",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "273",
		label: "ext jwt06 noTimestamp authenticate",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "274",
		label: "ext jwt02 spread alg authenticate",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "275",
		label: "ext jwt03 none handleAuth",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "276",
		label: "ext jwt04 ignoreExp handleAuth",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "277",
		label: "ext jwt06 noTimestamp handleAuth",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "278",
		label: "ext jwt02 spread alg handleAuth",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "279",
		label: "ext jwt03 none verifyRequest",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "280",
		label: "ext jwt04 ignoreExp verifyRequest",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "281",
		label: "ext jwt06 noTimestamp verifyRequest",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "282",
		label: "ext jwt02 spread alg verifyRequest",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "283",
		label: "ext jwt03 none issueCredentials",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "284",
		label: "ext jwt04 ignoreExp issueCredentials",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "285",
		label: "ext jwt06 noTimestamp issueCredentials",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "286",
		label: "ext jwt02 spread alg issueCredentials",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "287",
		label: "ext jwt03 none parseBearer",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "288",
		label: "ext jwt04 ignoreExp parseBearer",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "289",
		label: "ext jwt06 noTimestamp parseBearer",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "290",
		label: "ext jwt02 spread alg parseBearer",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "291",
		label: "ext jwt03 none validateSession",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "292",
		label: "ext jwt04 ignoreExp validateSession",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "293",
		label: "ext jwt06 noTimestamp validateSession",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "294",
		label: "ext jwt02 spread alg validateSession",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "295",
		label: "ext jwt03 none loginUser",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "296",
		label: "ext jwt04 ignoreExp loginUser",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "297",
		label: "ext jwt06 noTimestamp loginUser",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "298",
		label: "ext jwt02 spread alg loginUser",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "299",
		label: "ext jwt03 none middleware",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "300",
		label: "ext jwt04 ignoreExp middleware",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "301",
		label: "ext jwt06 noTimestamp middleware",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "302",
		label: "ext jwt02 spread alg middleware",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "303",
		label: "ext jwt03 none refreshToken",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "304",
		label: "ext jwt04 ignoreExp refreshToken",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "305",
		label: "ext jwt06 noTimestamp refreshToken",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "306",
		label: "ext jwt02 spread alg refreshToken",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "307",
		label: "ext jwt03 none decodeClaims",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "308",
		label: "ext jwt04 ignoreExp decodeClaims",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "309",
		label: "ext jwt06 noTimestamp decodeClaims",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "310",
		label: "ext jwt02 spread alg decodeClaims",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "311",
		label: "ext jwt03 none authenticate",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "312",
		label: "ext jwt04 ignoreExp authenticate",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "313",
		label: "ext jwt06 noTimestamp authenticate",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "314",
		label: "ext jwt02 spread alg authenticate",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "315",
		label: "ext jwt03 none handleAuth",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "316",
		label: "ext jwt04 ignoreExp handleAuth",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "317",
		label: "ext jwt06 noTimestamp handleAuth",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "318",
		label: "ext jwt02 spread alg handleAuth",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "319",
		label: "ext jwt03 none verifyRequest",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "320",
		label: "ext jwt04 ignoreExp verifyRequest",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "321",
		label: "ext jwt06 noTimestamp verifyRequest",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "322",
		label: "ext jwt02 spread alg verifyRequest",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "323",
		label: "ext jwt03 none issueCredentials",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "324",
		label: "ext jwt04 ignoreExp issueCredentials",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "325",
		label: "ext jwt06 noTimestamp issueCredentials",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "326",
		label: "ext jwt02 spread alg issueCredentials",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "327",
		label: "ext jwt03 none parseBearer",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "328",
		label: "ext jwt04 ignoreExp parseBearer",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "329",
		label: "ext jwt06 noTimestamp parseBearer",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "330",
		label: "ext jwt02 spread alg parseBearer",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "331",
		label: "ext jwt03 none validateSession",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "332",
		label: "ext jwt04 ignoreExp validateSession",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "333",
		label: "ext jwt06 noTimestamp validateSession",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "334",
		label: "ext jwt02 spread alg validateSession",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "335",
		label: "ext jwt03 none loginUser",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "336",
		label: "ext jwt04 ignoreExp loginUser",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "337",
		label: "ext jwt06 noTimestamp loginUser",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "338",
		label: "ext jwt02 spread alg loginUser",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "339",
		label: "ext jwt03 none middleware",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "340",
		label: "ext jwt04 ignoreExp middleware",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "341",
		label: "ext jwt06 noTimestamp middleware",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "342",
		label: "ext jwt02 spread alg middleware",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "343",
		label: "ext jwt03 none refreshToken",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "344",
		label: "ext jwt04 ignoreExp refreshToken",
		source:
			'const jwt = require("jsonwebtoken");\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "345",
		label: "ext jwt06 noTimestamp refreshToken",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "346",
		label: "ext jwt02 spread alg refreshToken",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "347",
		label: "ext jwt03 none decodeClaims",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "348",
		label: "ext jwt04 ignoreExp decodeClaims",
		source:
			'const jwt = require("jsonwebtoken");\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "349",
		label: "ext jwt06 noTimestamp decodeClaims",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "350",
		label: "ext jwt02 spread alg decodeClaims",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "351",
		label: 'JWT-01 decode-only import jwt from "jso use strict',
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "352",
		label: 'JWT-01 decode-only import jwt from "jso module banner',
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "353",
		label: 'JWT-01 decode-only import jwt from "jso export const',
		source:
			'import jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "354",
		label: 'JWT-01 decode-only import jwt from "jso async export',
		source:
			'import jwt from "jsonwebtoken";\nexport async function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "355",
		label: 'JWT-01 decode-only import jwt from "jso iife suffix',
		source:
			'import jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "356",
		label: "JWT-01 decode-only import * as jwt from use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "357",
		label: "JWT-01 decode-only import * as jwt from module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "358",
		label: "JWT-01 decode-only import * as jwt from export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "359",
		label: "JWT-01 decode-only import * as jwt from async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "360",
		label: "JWT-01 decode-only import * as jwt from iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "361",
		label: "JWT-01 decode-only import { decode, ver use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "362",
		label: "JWT-01 decode-only import { decode, ver module banner",
		source:
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "363",
		label: "JWT-01 decode-only import { decode, ver export const",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "364",
		label: "JWT-01 decode-only import { decode, ver async export",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport async function auth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "365",
		label: "JWT-01 decode-only import { decode, ver iife suffix",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "366",
		label: "JWT-01 decode-only const jwt = require( use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "367",
		label: "JWT-01 decode-only const jwt = require( module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "368",
		label: "JWT-01 decode-only const jwt = require( export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "369",
		label: "JWT-01 decode-only const jwt = require( async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "370",
		label: "JWT-01 decode-only const jwt = require( iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "371",
		label: "JWT-01 decode-only const jwt = require( use strict",
		source:
			'"use strict";\nconst jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {},
	},
	{
		id: "372",
		label: "JWT-01 decode-only const jwt = require( module banner",
		source:
			'/** module */\nconst jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {},
	},
	{
		id: "373",
		label: "JWT-01 decode-only const jwt = require( export const",
		source:
			'const jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "374",
		label: "JWT-01 decode-only const jwt = require( async export",
		source:
			'const jwt = require("node:jsonwebtoken");\nexport async function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {},
	},
	{
		id: "375",
		label: "JWT-01 decode-only const jwt = require( iife suffix",
		source:
			'const jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "376",
		label: "JWT-01 verify in inner fn suppresses decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: [\'HS256\'] }); }\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "377",
		label: "JWT-01 verify in inner fn suppresses decode module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "378",
		label: "JWT-01 verify in inner fn suppresses decode export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "379",
		label: "JWT-01 verify in inner fn suppresses decode async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "380",
		label: "JWT-01 verify in inner fn suppresses decode iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "381",
		label: "JWT-01 verify in outer only flags decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return inner(t);\n}',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "382",
		label: "JWT-01 verify in outer only flags decode module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}",
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "383",
		label: "JWT-01 verify in outer only flags decode export const",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "384",
		label: "JWT-01 verify in outer only flags decode async export",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport async function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}",
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "385",
		label: "JWT-01 verify in outer only flags decode iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "386",
		label: "JWT-01 direct helper verify suppresses use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "387",
		label: "JWT-01 direct helper verify suppresses module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "388",
		label: "JWT-01 direct helper verify suppresses export const",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "389",
		label: "JWT-01 direct helper verify suppresses async export",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport async function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}",
		expect: {},
	},
	{
		id: "390",
		label: "JWT-01 direct helper verify suppresses iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "391",
		label: "JWT-01 re-export verify suppresses decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "392",
		label: "JWT-01 re-export verify suppresses decode module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "393",
		label: "JWT-01 re-export verify suppresses decode export const",
		source:
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "394",
		label: "JWT-01 re-export verify suppresses decode async export",
		source:
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport async function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		expect: {},
	},
	{
		id: "395",
		label: "JWT-01 re-export verify suppresses decode iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "396",
		label: "JWT-02 jwt.verify(t, s) use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "397",
		label: "JWT-02 jwt.verify(t, s) module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "398",
		label: "JWT-02 jwt.verify(t, s) export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "399",
		label: "JWT-02 jwt.verify(t, s) async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "400",
		label: "JWT-02 jwt.verify(t, s) iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "401",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "402",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "403",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "404",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "405",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "406",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "407",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "408",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "409",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "410",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "411",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "412",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "413",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "414",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "415",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "416",
		label: "JWT-02 jwt.verify(t, s, { clockTolera use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "417",
		label: "JWT-02 jwt.verify(t, s, { clockTolera module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "418",
		label: "JWT-02 jwt.verify(t, s, { clockTolera export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "419",
		label: "JWT-02 jwt.verify(t, s, { clockTolera async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "420",
		label: "JWT-02 jwt.verify(t, s, { clockTolera iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "421",
		label: "JWT-02 jwt.verify(t, s, cb) use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "422",
		label: "JWT-02 jwt.verify(t, s, cb) module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "423",
		label: "JWT-02 jwt.verify(t, s, cb) export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "424",
		label: "JWT-02 jwt.verify(t, s, cb) async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "425",
		label: "JWT-02 jwt.verify(t, s, cb) iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "426",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		expect: {},
	},
	{
		id: "427",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "428",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "429",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "430",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "431",
		label: "JWT-02 jwt.verify(t, s) use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "432",
		label: "JWT-02 jwt.verify(t, s) module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "433",
		label: "JWT-02 jwt.verify(t, s) export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "434",
		label: "JWT-02 jwt.verify(t, s) async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "435",
		label: "JWT-02 jwt.verify(t, s) iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "436",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "437",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "438",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "439",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "440",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "441",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "442",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "443",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "444",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "445",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "446",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "447",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "448",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "449",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "450",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "451",
		label: "JWT-02 jwt.verify(t, s, { clockTolera use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "452",
		label: "JWT-02 jwt.verify(t, s, { clockTolera module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "453",
		label: "JWT-02 jwt.verify(t, s, { clockTolera export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "454",
		label: "JWT-02 jwt.verify(t, s, { clockTolera async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "455",
		label: "JWT-02 jwt.verify(t, s, { clockTolera iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "456",
		label: "JWT-02 jwt.verify(t, s, cb) use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "457",
		label: "JWT-02 jwt.verify(t, s, cb) module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "458",
		label: "JWT-02 jwt.verify(t, s, cb) export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "459",
		label: "JWT-02 jwt.verify(t, s, cb) async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "460",
		label: "JWT-02 jwt.verify(t, s, cb) iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "461",
		label: "JWT-02 jwt.verify(t, s, { algorithms: use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		expect: {},
	},
	{
		id: "462",
		label: "JWT-02 jwt.verify(t, s, { algorithms: module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "463",
		label: "JWT-02 jwt.verify(t, s, { algorithms: export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "464",
		label: "JWT-02 jwt.verify(t, s, { algorithms: async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "465",
		label: "JWT-02 jwt.verify(t, s, { algorithms: iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "466",
		label: "JWT-02 verify(t, s) use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "467",
		label: "JWT-02 verify(t, s) module banner",
		source:
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "468",
		label: "JWT-02 verify(t, s) export const",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "469",
		label: "JWT-02 verify(t, s) async export",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "470",
		label: "JWT-02 verify(t, s) iife suffix",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "471",
		label: "JWT-02 verify(t, s, { algorithms: ['H use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "472",
		label: "JWT-02 verify(t, s, { algorithms: ['H module banner",
		source:
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "473",
		label: "JWT-02 verify(t, s, { algorithms: ['H export const",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "474",
		label: "JWT-02 verify(t, s, { algorithms: ['H async export",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "475",
		label: "JWT-02 verify(t, s, { algorithms: ['H iife suffix",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "476",
		label: "JWT-02 verify(t, s, { algorithms: ['R use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "477",
		label: "JWT-02 verify(t, s, { algorithms: ['R module banner",
		source:
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "478",
		label: "JWT-02 verify(t, s, { algorithms: ['R export const",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "479",
		label: "JWT-02 verify(t, s, { algorithms: ['R async export",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "480",
		label: "JWT-02 verify(t, s, { algorithms: ['R iife suffix",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "481",
		label: "JWT-02 verify(t, s, { algorithms: ['H use strict",
		source:
			"\"use strict\";\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "482",
		label: "JWT-02 verify(t, s, { algorithms: ['H module banner",
		source:
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "483",
		label: "JWT-02 verify(t, s, { algorithms: ['H export const",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "484",
		label: "JWT-02 verify(t, s, { algorithms: ['H async export",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		expect: {},
	},
	{
		id: "485",
		label: "JWT-02 verify(t, s, { algorithms: ['H iife suffix",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "486",
		label: "JWT-02 verify(t, s, { clockTolerance: use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "487",
		label: "JWT-02 verify(t, s, { clockTolerance: module banner",
		source:
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "488",
		label: "JWT-02 verify(t, s, { clockTolerance: export const",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "489",
		label: "JWT-02 verify(t, s, { clockTolerance: async export",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "490",
		label: "JWT-02 verify(t, s, { clockTolerance: iife suffix",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "491",
		label: "JWT-02 verify(t, s, cb) use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "492",
		label: "JWT-02 verify(t, s, cb) module banner",
		source:
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "493",
		label: "JWT-02 verify(t, s, cb) export const",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "494",
		label: "JWT-02 verify(t, s, cb) async export",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		expect: {},
	},
	{
		id: "495",
		label: "JWT-02 verify(t, s, cb) iife suffix",
		source:
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "496",
		label: "JWT-02 verify(t, s, { algorithms: ['H use strict",
		source:
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		expect: {},
	},
	{
		id: "497",
		label: "JWT-02 verify(t, s, { algorithms: ['H module banner",
		source:
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "498",
		label: "JWT-02 verify(t, s, { algorithms: ['H export const",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "499",
		label: "JWT-02 verify(t, s, { algorithms: ['H async export",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport async function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		expect: {},
	},
	{
		id: "500",
		label: "JWT-02 verify(t, s, { algorithms: ['H iife suffix",
		source:
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "501",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none']  use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "502",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none']  module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "503",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none']  export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "504",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none']  async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "505",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['none']  iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "506",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256', use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "507",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256', module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "508",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256', export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "509",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256', async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "510",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256', iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "511",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "512",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "513",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "514",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "515",
		label: "JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
			"CS-JWT-05": 1,
		},
	},
	{
		id: "516",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "517",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "518",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "519",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "520",
		label: "JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "521",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "522",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "523",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "524",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "525",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: tru iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "526",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "527",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "528",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "529",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "530",
		label: "JWT-04 jwt.verify(t, s, { ignoreExpiration: fal iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "531",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'], ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "532",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n",
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "533",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "534",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n",
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "535",
		label: "JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-04": 1,
		},
	},
	{
		id: "536",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "537",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "538",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "539",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "540",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "541",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "542",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "543",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "544",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "545",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "546",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s);\n}\n',
		expect: {},
	},
	{
		id: "547",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "548",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "549",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "550",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "551",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "552",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "553",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "554",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "555",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "556",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s, cb);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "557",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "558",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "559",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "560",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "561",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "562",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "563",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "564",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "565",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s) iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "566",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires use strict",
		source:
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "567",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "568",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "569",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "570",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { expires iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "571",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s);\n}\n',
		expect: {},
	},
	{
		id: "572",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "573",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "574",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		expect: {},
	},
	{
		id: "575",
		label: "JWT-05 jwt.sign({ sub: 'u', exp: 999999999 iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "576",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit use strict",
		source:
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "577",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "578",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "579",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "580",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, { algorit iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "581",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s, cb);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "582",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "583",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "584",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "585",
		label: "JWT-05 jwt.sign({ sub: 'u' }, s, cb) iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "586",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "587",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "588",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "589",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "590",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "591",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: use strict",
		source:
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "592",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "593",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "594",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		expect: {},
	},
	{
		id: "595",
		label: "JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "596",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s, { noTimestamp: true });\n}\n',
		expect: {},
	},
	{
		id: "597",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n",
		expect: {},
	},
	{
		id: "598",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "599",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n",
		expect: {},
	},
	{
		id: "600",
		label: "JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "601",
		label: "JWT-01 destructured decode not tracked use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		expect: {},
	},
	{
		id: "602",
		label: "JWT-01 destructured decode not tracked module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		expect: {},
	},
	{
		id: "603",
		label: "JWT-01 destructured decode not tracked export const",
		source:
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "604",
		label: "JWT-01 destructured decode not tracked async export",
		source:
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport async function f(t: string) { return decode(t); }',
		expect: {},
	},
	{
		id: "605",
		label: "JWT-01 destructured decode not tracked iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "606",
		label: "JWT-01 verify-only clean use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		expect: {},
	},
	{
		id: "607",
		label: "JWT-01 verify-only clean module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		expect: {},
	},
	{
		id: "608",
		label: "JWT-01 verify-only clean export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "609",
		label: "JWT-01 verify-only clean async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		expect: {},
	},
	{
		id: "610",
		label: "JWT-01 verify-only clean iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "611",
		label: "JWT-01 arrow decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "612",
		label: "JWT-01 arrow decode module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "613",
		label: "JWT-01 arrow decode export const",
		source:
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "614",
		label: "JWT-01 arrow decode async export",
		source:
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "615",
		label: "JWT-01 arrow decode iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "616",
		label: "JWT-01 async decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "617",
		label: "JWT-01 async decode module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "618",
		label: "JWT-01 async decode export const",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "619",
		label: "JWT-01 async decode async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "620",
		label: "JWT-01 async decode iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "621",
		label: "JWT-01 class method decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "622",
		label: "JWT-01 class method decode module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "623",
		label: "JWT-01 class method decode export const",
		source:
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "624",
		label: "JWT-01 class method decode async export",
		source:
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "625",
		label: "JWT-01 class method decode iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "626",
		label: "JWT-01 IIFE decode use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "627",
		label: "JWT-01 IIFE decode module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "628",
		label: "JWT-01 IIFE decode export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "629",
		label: "JWT-01 IIFE decode async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return (() => jwt.decode(t))(); }\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "630",
		label: "JWT-01 IIFE decode iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "631",
		label: "JWT-02 spread algorithms not explicit use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { algorithms: [\'HS256\'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "632",
		label: "JWT-02 spread algorithms not explicit module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}",
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "633",
		label: "JWT-02 spread algorithms not explicit export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "634",
		label: "JWT-02 spread algorithms not explicit async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}",
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "635",
		label: "JWT-02 spread algorithms not explicit iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "636",
		label: "JWT-02 empty algorithms array use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "637",
		label: "JWT-02 empty algorithms array module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "638",
		label: "JWT-02 empty algorithms array export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "639",
		label: "JWT-02 empty algorithms array async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "640",
		label: "JWT-02 empty algorithms array iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "641",
		label: "JWT-05 two-arg sign secret variant use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "642",
		label: "JWT-05 two-arg sign secret variant module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "643",
		label: "JWT-05 two-arg sign secret variant export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "644",
		label: "JWT-05 two-arg sign secret variant async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "645",
		label: "JWT-05 two-arg sign secret variant iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "646",
		label: "JWT-05 expiresIn clean variant use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "647",
		label: "JWT-05 expiresIn clean variant module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "648",
		label: "JWT-05 expiresIn clean variant export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "649",
		label: "JWT-05 expiresIn clean variant async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "650",
		label: "JWT-05 expiresIn clean variant iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "651",
		label: "JWT-05 two-arg sign secret variant use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "652",
		label: "JWT-05 two-arg sign secret variant module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "653",
		label: "JWT-05 two-arg sign secret variant export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "654",
		label: "JWT-05 two-arg sign secret variant async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "655",
		label: "JWT-05 two-arg sign secret variant iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "656",
		label: "JWT-05 expiresIn clean variant use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "657",
		label: "JWT-05 expiresIn clean variant module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "658",
		label: "JWT-05 expiresIn clean variant export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\nexport const MODULE = 1;\n',
		expect: {},
	},
	{
		id: "659",
		label: "JWT-05 expiresIn clean variant async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		expect: {},
	},
	{
		id: "660",
		label: "JWT-05 expiresIn clean variant iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {},
	},
	{
		id: "661",
		label: "grid decode authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "662",
		label: "grid decode authenticate module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "663",
		label: "grid decode authenticate export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "664",
		label: "grid decode authenticate async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "665",
		label: "grid decode authenticate iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "666",
		label: "grid verify-no-alg authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "667",
		label: "grid verify-no-alg authenticate module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "668",
		label: "grid verify-no-alg authenticate export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "669",
		label: "grid verify-no-alg authenticate async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "670",
		label: "grid verify-no-alg authenticate iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "671",
		label: "grid verify-alg authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "672",
		label: "grid verify-alg authenticate module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "673",
		label: "grid verify-alg authenticate export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "674",
		label: "grid verify-alg authenticate async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "675",
		label: "grid verify-alg authenticate iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "676",
		label: "grid sign-no-exp authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "677",
		label: "grid sign-no-exp authenticate module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "678",
		label: "grid sign-no-exp authenticate export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "679",
		label: "grid sign-no-exp authenticate async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "680",
		label: "grid sign-no-exp authenticate iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "681",
		label: "grid decode handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "682",
		label: "grid decode handleAuth module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "683",
		label: "grid decode handleAuth export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "684",
		label: "grid decode handleAuth async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "685",
		label: "grid decode handleAuth iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "686",
		label: "grid verify-no-alg handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "687",
		label: "grid verify-no-alg handleAuth module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "688",
		label: "grid verify-no-alg handleAuth export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "689",
		label: "grid verify-no-alg handleAuth async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "690",
		label: "grid verify-no-alg handleAuth iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "691",
		label: "grid verify-alg handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "692",
		label: "grid verify-alg handleAuth module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "693",
		label: "grid verify-alg handleAuth export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "694",
		label: "grid verify-alg handleAuth async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "695",
		label: "grid verify-alg handleAuth iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "696",
		label: "grid sign-no-exp handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "697",
		label: "grid sign-no-exp handleAuth module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "698",
		label: "grid sign-no-exp handleAuth export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "699",
		label: "grid sign-no-exp handleAuth async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "700",
		label: "grid sign-no-exp handleAuth iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "701",
		label: "grid decode verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "702",
		label: "grid decode verifyRequest module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "703",
		label: "grid decode verifyRequest export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "704",
		label: "grid decode verifyRequest async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "705",
		label: "grid decode verifyRequest iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "706",
		label: "grid verify-no-alg verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "707",
		label: "grid verify-no-alg verifyRequest module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "708",
		label: "grid verify-no-alg verifyRequest export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "709",
		label: "grid verify-no-alg verifyRequest async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "710",
		label: "grid verify-no-alg verifyRequest iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "711",
		label: "grid verify-alg verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "712",
		label: "grid verify-alg verifyRequest module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "713",
		label: "grid verify-alg verifyRequest export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "714",
		label: "grid verify-alg verifyRequest async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "715",
		label: "grid verify-alg verifyRequest iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "716",
		label: "grid sign-no-exp verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "717",
		label: "grid sign-no-exp verifyRequest module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "718",
		label: "grid sign-no-exp verifyRequest export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "719",
		label: "grid sign-no-exp verifyRequest async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "720",
		label: "grid sign-no-exp verifyRequest iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "721",
		label: "grid decode issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "722",
		label: "grid decode issueCredentials module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "723",
		label: "grid decode issueCredentials export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "724",
		label: "grid decode issueCredentials async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "725",
		label: "grid decode issueCredentials iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "726",
		label: "grid verify-no-alg issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "727",
		label: "grid verify-no-alg issueCredentials module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "728",
		label: "grid verify-no-alg issueCredentials export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "729",
		label: "grid verify-no-alg issueCredentials async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "730",
		label: "grid verify-no-alg issueCredentials iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "731",
		label: "grid verify-alg issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "732",
		label: "grid verify-alg issueCredentials module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "733",
		label: "grid verify-alg issueCredentials export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "734",
		label: "grid verify-alg issueCredentials async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "735",
		label: "grid verify-alg issueCredentials iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "736",
		label: "grid sign-no-exp issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "737",
		label: "grid sign-no-exp issueCredentials module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "738",
		label: "grid sign-no-exp issueCredentials export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "739",
		label: "grid sign-no-exp issueCredentials async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "740",
		label: "grid sign-no-exp issueCredentials iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "741",
		label: "grid decode parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "742",
		label: "grid decode parseBearer module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "743",
		label: "grid decode parseBearer export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "744",
		label: "grid decode parseBearer async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "745",
		label: "grid decode parseBearer iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "746",
		label: "grid verify-no-alg parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "747",
		label: "grid verify-no-alg parseBearer module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "748",
		label: "grid verify-no-alg parseBearer export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "749",
		label: "grid verify-no-alg parseBearer async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "750",
		label: "grid verify-no-alg parseBearer iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "751",
		label: "grid verify-alg parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "752",
		label: "grid verify-alg parseBearer module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "753",
		label: "grid verify-alg parseBearer export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "754",
		label: "grid verify-alg parseBearer async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "755",
		label: "grid verify-alg parseBearer iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "756",
		label: "grid sign-no-exp parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "757",
		label: "grid sign-no-exp parseBearer module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "758",
		label: "grid sign-no-exp parseBearer export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "759",
		label: "grid sign-no-exp parseBearer async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "760",
		label: "grid sign-no-exp parseBearer iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "761",
		label: "grid decode validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "762",
		label: "grid decode validateSession module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "763",
		label: "grid decode validateSession export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "764",
		label: "grid decode validateSession async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "765",
		label: "grid decode validateSession iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "766",
		label: "grid verify-no-alg validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "767",
		label: "grid verify-no-alg validateSession module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "768",
		label: "grid verify-no-alg validateSession export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "769",
		label: "grid verify-no-alg validateSession async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "770",
		label: "grid verify-no-alg validateSession iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "771",
		label: "grid verify-alg validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "772",
		label: "grid verify-alg validateSession module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "773",
		label: "grid verify-alg validateSession export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "774",
		label: "grid verify-alg validateSession async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "775",
		label: "grid verify-alg validateSession iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "776",
		label: "grid sign-no-exp validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "777",
		label: "grid sign-no-exp validateSession module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "778",
		label: "grid sign-no-exp validateSession export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "779",
		label: "grid sign-no-exp validateSession async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "780",
		label: "grid sign-no-exp validateSession iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "781",
		label: "grid decode loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "782",
		label: "grid decode loginUser module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "783",
		label: "grid decode loginUser export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "784",
		label: "grid decode loginUser async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "785",
		label: "grid decode loginUser iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "786",
		label: "grid verify-no-alg loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "787",
		label: "grid verify-no-alg loginUser module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "788",
		label: "grid verify-no-alg loginUser export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "789",
		label: "grid verify-no-alg loginUser async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "790",
		label: "grid verify-no-alg loginUser iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "791",
		label: "grid verify-alg loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "792",
		label: "grid verify-alg loginUser module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "793",
		label: "grid verify-alg loginUser export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "794",
		label: "grid verify-alg loginUser async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "795",
		label: "grid verify-alg loginUser iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "796",
		label: "grid sign-no-exp loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "797",
		label: "grid sign-no-exp loginUser module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "798",
		label: "grid sign-no-exp loginUser export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "799",
		label: "grid sign-no-exp loginUser async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "800",
		label: "grid sign-no-exp loginUser iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "801",
		label: "grid decode middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "802",
		label: "grid decode middleware module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "803",
		label: "grid decode middleware export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "804",
		label: "grid decode middleware async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "805",
		label: "grid decode middleware iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "806",
		label: "grid verify-no-alg middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "807",
		label: "grid verify-no-alg middleware module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "808",
		label: "grid verify-no-alg middleware export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "809",
		label: "grid verify-no-alg middleware async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "810",
		label: "grid verify-no-alg middleware iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "811",
		label: "grid verify-alg middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "812",
		label: "grid verify-alg middleware module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "813",
		label: "grid verify-alg middleware export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "814",
		label: "grid verify-alg middleware async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "815",
		label: "grid verify-alg middleware iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "816",
		label: "grid sign-no-exp middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "817",
		label: "grid sign-no-exp middleware module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "818",
		label: "grid sign-no-exp middleware export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "819",
		label: "grid sign-no-exp middleware async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "820",
		label: "grid sign-no-exp middleware iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "821",
		label: "grid decode authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "822",
		label: "grid decode authenticate module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "823",
		label: "grid decode authenticate export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "824",
		label: "grid decode authenticate async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "825",
		label: "grid decode authenticate iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "826",
		label: "grid verify-no-alg authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "827",
		label: "grid verify-no-alg authenticate module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "828",
		label: "grid verify-no-alg authenticate export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "829",
		label: "grid verify-no-alg authenticate async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "830",
		label: "grid verify-no-alg authenticate iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "831",
		label: "grid verify-alg authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "832",
		label: "grid verify-alg authenticate module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "833",
		label: "grid verify-alg authenticate export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "834",
		label: "grid verify-alg authenticate async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "835",
		label: "grid verify-alg authenticate iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "836",
		label: "grid sign-no-exp authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "837",
		label: "grid sign-no-exp authenticate module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "838",
		label: "grid sign-no-exp authenticate export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "839",
		label: "grid sign-no-exp authenticate async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "840",
		label: "grid sign-no-exp authenticate iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "841",
		label: "grid decode handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "842",
		label: "grid decode handleAuth module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "843",
		label: "grid decode handleAuth export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "844",
		label: "grid decode handleAuth async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "845",
		label: "grid decode handleAuth iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "846",
		label: "grid verify-no-alg handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "847",
		label: "grid verify-no-alg handleAuth module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "848",
		label: "grid verify-no-alg handleAuth export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "849",
		label: "grid verify-no-alg handleAuth async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "850",
		label: "grid verify-no-alg handleAuth iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "851",
		label: "grid verify-alg handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "852",
		label: "grid verify-alg handleAuth module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "853",
		label: "grid verify-alg handleAuth export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "854",
		label: "grid verify-alg handleAuth async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "855",
		label: "grid verify-alg handleAuth iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "856",
		label: "grid sign-no-exp handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "857",
		label: "grid sign-no-exp handleAuth module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "858",
		label: "grid sign-no-exp handleAuth export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "859",
		label: "grid sign-no-exp handleAuth async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "860",
		label: "grid sign-no-exp handleAuth iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "861",
		label: "grid decode verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "862",
		label: "grid decode verifyRequest module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "863",
		label: "grid decode verifyRequest export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "864",
		label: "grid decode verifyRequest async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "865",
		label: "grid decode verifyRequest iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "866",
		label: "grid verify-no-alg verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "867",
		label: "grid verify-no-alg verifyRequest module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "868",
		label: "grid verify-no-alg verifyRequest export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "869",
		label: "grid verify-no-alg verifyRequest async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "870",
		label: "grid verify-no-alg verifyRequest iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "871",
		label: "grid verify-alg verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "872",
		label: "grid verify-alg verifyRequest module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "873",
		label: "grid verify-alg verifyRequest export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "874",
		label: "grid verify-alg verifyRequest async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "875",
		label: "grid verify-alg verifyRequest iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "876",
		label: "grid sign-no-exp verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "877",
		label: "grid sign-no-exp verifyRequest module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "878",
		label: "grid sign-no-exp verifyRequest export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "879",
		label: "grid sign-no-exp verifyRequest async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "880",
		label: "grid sign-no-exp verifyRequest iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "881",
		label: "grid decode issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "882",
		label: "grid decode issueCredentials module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "883",
		label: "grid decode issueCredentials export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "884",
		label: "grid decode issueCredentials async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "885",
		label: "grid decode issueCredentials iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "886",
		label: "grid verify-no-alg issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "887",
		label: "grid verify-no-alg issueCredentials module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "888",
		label: "grid verify-no-alg issueCredentials export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "889",
		label: "grid verify-no-alg issueCredentials async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "890",
		label: "grid verify-no-alg issueCredentials iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "891",
		label: "grid verify-alg issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "892",
		label: "grid verify-alg issueCredentials module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "893",
		label: "grid verify-alg issueCredentials export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "894",
		label: "grid verify-alg issueCredentials async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "895",
		label: "grid verify-alg issueCredentials iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "896",
		label: "grid sign-no-exp issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "897",
		label: "grid sign-no-exp issueCredentials module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "898",
		label: "grid sign-no-exp issueCredentials export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "899",
		label: "grid sign-no-exp issueCredentials async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "900",
		label: "grid sign-no-exp issueCredentials iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "901",
		label: "grid decode parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "902",
		label: "grid decode parseBearer module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "903",
		label: "grid decode parseBearer export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "904",
		label: "grid decode parseBearer async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "905",
		label: "grid decode parseBearer iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "906",
		label: "grid verify-no-alg parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "907",
		label: "grid verify-no-alg parseBearer module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "908",
		label: "grid verify-no-alg parseBearer export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "909",
		label: "grid verify-no-alg parseBearer async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "910",
		label: "grid verify-no-alg parseBearer iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "911",
		label: "grid verify-alg parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "912",
		label: "grid verify-alg parseBearer module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "913",
		label: "grid verify-alg parseBearer export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "914",
		label: "grid verify-alg parseBearer async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "915",
		label: "grid verify-alg parseBearer iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "916",
		label: "grid sign-no-exp parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "917",
		label: "grid sign-no-exp parseBearer module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "918",
		label: "grid sign-no-exp parseBearer export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "919",
		label: "grid sign-no-exp parseBearer async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "920",
		label: "grid sign-no-exp parseBearer iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "921",
		label: "grid decode validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "922",
		label: "grid decode validateSession module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "923",
		label: "grid decode validateSession export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "924",
		label: "grid decode validateSession async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "925",
		label: "grid decode validateSession iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "926",
		label: "grid verify-no-alg validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "927",
		label: "grid verify-no-alg validateSession module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "928",
		label: "grid verify-no-alg validateSession export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "929",
		label: "grid verify-no-alg validateSession async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "930",
		label: "grid verify-no-alg validateSession iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "931",
		label: "grid verify-alg validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "932",
		label: "grid verify-alg validateSession module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "933",
		label: "grid verify-alg validateSession export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "934",
		label: "grid verify-alg validateSession async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "935",
		label: "grid verify-alg validateSession iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "936",
		label: "grid sign-no-exp validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "937",
		label: "grid sign-no-exp validateSession module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "938",
		label: "grid sign-no-exp validateSession export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "939",
		label: "grid sign-no-exp validateSession async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "940",
		label: "grid sign-no-exp validateSession iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "941",
		label: "grid decode loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "942",
		label: "grid decode loginUser module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "943",
		label: "grid decode loginUser export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "944",
		label: "grid decode loginUser async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "945",
		label: "grid decode loginUser iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "946",
		label: "grid verify-no-alg loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "947",
		label: "grid verify-no-alg loginUser module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "948",
		label: "grid verify-no-alg loginUser export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "949",
		label: "grid verify-no-alg loginUser async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "950",
		label: "grid verify-no-alg loginUser iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "951",
		label: "grid verify-alg loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "952",
		label: "grid verify-alg loginUser module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "953",
		label: "grid verify-alg loginUser export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "954",
		label: "grid verify-alg loginUser async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "955",
		label: "grid verify-alg loginUser iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "956",
		label: "grid sign-no-exp loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "957",
		label: "grid sign-no-exp loginUser module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "958",
		label: "grid sign-no-exp loginUser export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "959",
		label: "grid sign-no-exp loginUser async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "960",
		label: "grid sign-no-exp loginUser iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "961",
		label: "grid decode middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "962",
		label: "grid decode middleware module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "963",
		label: "grid decode middleware export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "964",
		label: "grid decode middleware async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "965",
		label: "grid decode middleware iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "966",
		label: "grid verify-no-alg middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "967",
		label: "grid verify-no-alg middleware module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "968",
		label: "grid verify-no-alg middleware export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "969",
		label: "grid verify-no-alg middleware async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "970",
		label: "grid verify-no-alg middleware iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "971",
		label: "grid verify-alg middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "972",
		label: "grid verify-alg middleware module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "973",
		label: "grid verify-alg middleware export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "974",
		label: "grid verify-alg middleware async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "975",
		label: "grid verify-alg middleware iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "976",
		label: "grid sign-no-exp middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "977",
		label: "grid sign-no-exp middleware module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "978",
		label: "grid sign-no-exp middleware export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "979",
		label: "grid sign-no-exp middleware async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "980",
		label: "grid sign-no-exp middleware iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "981",
		label: "grid decode authenticate use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "982",
		label: "grid decode authenticate module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "983",
		label: "grid decode authenticate export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "984",
		label: "grid decode authenticate async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function authenticate(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "985",
		label: "grid decode authenticate iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "986",
		label: "grid verify-no-alg authenticate use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "987",
		label: "grid verify-no-alg authenticate module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "988",
		label: "grid verify-no-alg authenticate export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "989",
		label: "grid verify-no-alg authenticate async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "990",
		label: "grid verify-no-alg authenticate iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "991",
		label: "grid verify-alg authenticate use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "992",
		label: "grid verify-alg authenticate module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "993",
		label: "grid verify-alg authenticate export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "994",
		label: "grid verify-alg authenticate async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "995",
		label: "grid verify-alg authenticate iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "996",
		label: "grid sign-no-exp authenticate use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "997",
		label: "grid sign-no-exp authenticate module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "998",
		label: "grid sign-no-exp authenticate export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "999",
		label: "grid sign-no-exp authenticate async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1000",
		label: "grid sign-no-exp authenticate iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1001",
		label: "grid decode handleAuth use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1002",
		label: "grid decode handleAuth module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1003",
		label: "grid decode handleAuth export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1004",
		label: "grid decode handleAuth async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function handleAuth(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1005",
		label: "grid decode handleAuth iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1006",
		label: "grid verify-no-alg handleAuth use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1007",
		label: "grid verify-no-alg handleAuth module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1008",
		label: "grid verify-no-alg handleAuth export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1009",
		label: "grid verify-no-alg handleAuth async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1010",
		label: "grid verify-no-alg handleAuth iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1011",
		label: "grid verify-alg handleAuth use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1012",
		label: "grid verify-alg handleAuth module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1013",
		label: "grid verify-alg handleAuth export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1014",
		label: "grid verify-alg handleAuth async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1015",
		label: "grid verify-alg handleAuth iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1016",
		label: "grid sign-no-exp handleAuth use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1017",
		label: "grid sign-no-exp handleAuth module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1018",
		label: "grid sign-no-exp handleAuth export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1019",
		label: "grid sign-no-exp handleAuth async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1020",
		label: "grid sign-no-exp handleAuth iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1021",
		label: "grid decode verifyRequest use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1022",
		label: "grid decode verifyRequest module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1023",
		label: "grid decode verifyRequest export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1024",
		label: "grid decode verifyRequest async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1025",
		label: "grid decode verifyRequest iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1026",
		label: "grid verify-no-alg verifyRequest use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1027",
		label: "grid verify-no-alg verifyRequest module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1028",
		label: "grid verify-no-alg verifyRequest export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1029",
		label: "grid verify-no-alg verifyRequest async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1030",
		label: "grid verify-no-alg verifyRequest iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1031",
		label: "grid verify-alg verifyRequest use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1032",
		label: "grid verify-alg verifyRequest module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1033",
		label: "grid verify-alg verifyRequest export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1034",
		label: "grid verify-alg verifyRequest async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1035",
		label: "grid verify-alg verifyRequest iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1036",
		label: "grid sign-no-exp verifyRequest use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1037",
		label: "grid sign-no-exp verifyRequest module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1038",
		label: "grid sign-no-exp verifyRequest export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1039",
		label: "grid sign-no-exp verifyRequest async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1040",
		label: "grid sign-no-exp verifyRequest iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1041",
		label: "grid decode issueCredentials use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1042",
		label: "grid decode issueCredentials module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1043",
		label: "grid decode issueCredentials export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1044",
		label: "grid decode issueCredentials async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1045",
		label: "grid decode issueCredentials iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1046",
		label: "grid verify-no-alg issueCredentials use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1047",
		label: "grid verify-no-alg issueCredentials module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1048",
		label: "grid verify-no-alg issueCredentials export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1049",
		label: "grid verify-no-alg issueCredentials async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1050",
		label: "grid verify-no-alg issueCredentials iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1051",
		label: "grid verify-alg issueCredentials use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1052",
		label: "grid verify-alg issueCredentials module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1053",
		label: "grid verify-alg issueCredentials export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1054",
		label: "grid verify-alg issueCredentials async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1055",
		label: "grid verify-alg issueCredentials iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1056",
		label: "grid sign-no-exp issueCredentials use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1057",
		label: "grid sign-no-exp issueCredentials module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1058",
		label: "grid sign-no-exp issueCredentials export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1059",
		label: "grid sign-no-exp issueCredentials async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1060",
		label: "grid sign-no-exp issueCredentials iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1061",
		label: "grid decode parseBearer use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1062",
		label: "grid decode parseBearer module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1063",
		label: "grid decode parseBearer export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1064",
		label: "grid decode parseBearer async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function parseBearer(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1065",
		label: "grid decode parseBearer iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1066",
		label: "grid verify-no-alg parseBearer use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1067",
		label: "grid verify-no-alg parseBearer module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1068",
		label: "grid verify-no-alg parseBearer export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1069",
		label: "grid verify-no-alg parseBearer async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1070",
		label: "grid verify-no-alg parseBearer iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1071",
		label: "grid verify-alg parseBearer use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1072",
		label: "grid verify-alg parseBearer module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1073",
		label: "grid verify-alg parseBearer export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1074",
		label: "grid verify-alg parseBearer async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1075",
		label: "grid verify-alg parseBearer iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1076",
		label: "grid sign-no-exp parseBearer use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1077",
		label: "grid sign-no-exp parseBearer module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1078",
		label: "grid sign-no-exp parseBearer export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1079",
		label: "grid sign-no-exp parseBearer async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1080",
		label: "grid sign-no-exp parseBearer iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1081",
		label: "grid decode validateSession use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1082",
		label: "grid decode validateSession module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1083",
		label: "grid decode validateSession export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1084",
		label: "grid decode validateSession async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function validateSession(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1085",
		label: "grid decode validateSession iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1086",
		label: "grid verify-no-alg validateSession use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1087",
		label: "grid verify-no-alg validateSession module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1088",
		label: "grid verify-no-alg validateSession export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1089",
		label: "grid verify-no-alg validateSession async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1090",
		label: "grid verify-no-alg validateSession iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1091",
		label: "grid verify-alg validateSession use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1092",
		label: "grid verify-alg validateSession module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1093",
		label: "grid verify-alg validateSession export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1094",
		label: "grid verify-alg validateSession async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1095",
		label: "grid verify-alg validateSession iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1096",
		label: "grid sign-no-exp validateSession use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1097",
		label: "grid sign-no-exp validateSession module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1098",
		label: "grid sign-no-exp validateSession export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1099",
		label: "grid sign-no-exp validateSession async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1100",
		label: "grid sign-no-exp validateSession iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1101",
		label: "grid decode loginUser use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1102",
		label: "grid decode loginUser module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1103",
		label: "grid decode loginUser export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1104",
		label: "grid decode loginUser async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function loginUser(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1105",
		label: "grid decode loginUser iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1106",
		label: "grid verify-no-alg loginUser use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1107",
		label: "grid verify-no-alg loginUser module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1108",
		label: "grid verify-no-alg loginUser export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1109",
		label: "grid verify-no-alg loginUser async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1110",
		label: "grid verify-no-alg loginUser iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1111",
		label: "grid verify-alg loginUser use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1112",
		label: "grid verify-alg loginUser module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1113",
		label: "grid verify-alg loginUser export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1114",
		label: "grid verify-alg loginUser async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1115",
		label: "grid verify-alg loginUser iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1116",
		label: "grid sign-no-exp loginUser use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1117",
		label: "grid sign-no-exp loginUser module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1118",
		label: "grid sign-no-exp loginUser export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1119",
		label: "grid sign-no-exp loginUser async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1120",
		label: "grid sign-no-exp loginUser iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1121",
		label: "grid decode middleware use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1122",
		label: "grid decode middleware module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1123",
		label: "grid decode middleware export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1124",
		label: "grid decode middleware async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function middleware(t: string) {\n  return decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1125",
		label: "grid decode middleware iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1126",
		label: "grid verify-no-alg middleware use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1127",
		label: "grid verify-no-alg middleware module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1128",
		label: "grid verify-no-alg middleware export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1129",
		label: "grid verify-no-alg middleware async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1130",
		label: "grid verify-no-alg middleware iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1131",
		label: "grid verify-alg middleware use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1132",
		label: "grid verify-alg middleware module banner",
		source:
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1133",
		label: "grid verify-alg middleware export const",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1134",
		label: "grid verify-alg middleware async export",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport async function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1135",
		label: "grid verify-alg middleware iife suffix",
		source:
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1136",
		label: "grid sign-no-exp middleware use strict",
		source:
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1137",
		label: "grid sign-no-exp middleware module banner",
		source:
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1138",
		label: "grid sign-no-exp middleware export const",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1139",
		label: "grid sign-no-exp middleware async export",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport async function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1140",
		label: "grid sign-no-exp middleware iife suffix",
		source:
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1141",
		label: "grid decode authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1142",
		label: "grid decode authenticate module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1143",
		label: "grid decode authenticate export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1144",
		label: "grid decode authenticate async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1145",
		label: "grid decode authenticate iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1146",
		label: "grid verify-no-alg authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1147",
		label: "grid verify-no-alg authenticate module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1148",
		label: "grid verify-no-alg authenticate export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1149",
		label: "grid verify-no-alg authenticate async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1150",
		label: "grid verify-no-alg authenticate iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1151",
		label: "grid verify-alg authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1152",
		label: "grid verify-alg authenticate module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1153",
		label: "grid verify-alg authenticate export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1154",
		label: "grid verify-alg authenticate async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1155",
		label: "grid verify-alg authenticate iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1156",
		label: "grid sign-no-exp authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1157",
		label: "grid sign-no-exp authenticate module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1158",
		label: "grid sign-no-exp authenticate export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1159",
		label: "grid sign-no-exp authenticate async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1160",
		label: "grid sign-no-exp authenticate iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1161",
		label: "grid decode handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1162",
		label: "grid decode handleAuth module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1163",
		label: "grid decode handleAuth export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1164",
		label: "grid decode handleAuth async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1165",
		label: "grid decode handleAuth iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1166",
		label: "grid verify-no-alg handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1167",
		label: "grid verify-no-alg handleAuth module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1168",
		label: "grid verify-no-alg handleAuth export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1169",
		label: "grid verify-no-alg handleAuth async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1170",
		label: "grid verify-no-alg handleAuth iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1171",
		label: "grid verify-alg handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1172",
		label: "grid verify-alg handleAuth module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1173",
		label: "grid verify-alg handleAuth export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1174",
		label: "grid verify-alg handleAuth async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1175",
		label: "grid verify-alg handleAuth iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1176",
		label: "grid sign-no-exp handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1177",
		label: "grid sign-no-exp handleAuth module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1178",
		label: "grid sign-no-exp handleAuth export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1179",
		label: "grid sign-no-exp handleAuth async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1180",
		label: "grid sign-no-exp handleAuth iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1181",
		label: "grid decode verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1182",
		label: "grid decode verifyRequest module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1183",
		label: "grid decode verifyRequest export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1184",
		label: "grid decode verifyRequest async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1185",
		label: "grid decode verifyRequest iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1186",
		label: "grid verify-no-alg verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1187",
		label: "grid verify-no-alg verifyRequest module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1188",
		label: "grid verify-no-alg verifyRequest export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1189",
		label: "grid verify-no-alg verifyRequest async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1190",
		label: "grid verify-no-alg verifyRequest iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1191",
		label: "grid verify-alg verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1192",
		label: "grid verify-alg verifyRequest module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1193",
		label: "grid verify-alg verifyRequest export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1194",
		label: "grid verify-alg verifyRequest async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1195",
		label: "grid verify-alg verifyRequest iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1196",
		label: "grid sign-no-exp verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1197",
		label: "grid sign-no-exp verifyRequest module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1198",
		label: "grid sign-no-exp verifyRequest export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1199",
		label: "grid sign-no-exp verifyRequest async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1200",
		label: "grid sign-no-exp verifyRequest iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1201",
		label: "grid decode issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1202",
		label: "grid decode issueCredentials module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1203",
		label: "grid decode issueCredentials export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1204",
		label: "grid decode issueCredentials async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1205",
		label: "grid decode issueCredentials iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1206",
		label: "grid verify-no-alg issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1207",
		label: "grid verify-no-alg issueCredentials module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1208",
		label: "grid verify-no-alg issueCredentials export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1209",
		label: "grid verify-no-alg issueCredentials async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1210",
		label: "grid verify-no-alg issueCredentials iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1211",
		label: "grid verify-alg issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1212",
		label: "grid verify-alg issueCredentials module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1213",
		label: "grid verify-alg issueCredentials export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1214",
		label: "grid verify-alg issueCredentials async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1215",
		label: "grid verify-alg issueCredentials iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1216",
		label: "grid sign-no-exp issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1217",
		label: "grid sign-no-exp issueCredentials module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1218",
		label: "grid sign-no-exp issueCredentials export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1219",
		label: "grid sign-no-exp issueCredentials async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1220",
		label: "grid sign-no-exp issueCredentials iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1221",
		label: "grid decode parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1222",
		label: "grid decode parseBearer module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1223",
		label: "grid decode parseBearer export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1224",
		label: "grid decode parseBearer async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1225",
		label: "grid decode parseBearer iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1226",
		label: "grid verify-no-alg parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1227",
		label: "grid verify-no-alg parseBearer module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1228",
		label: "grid verify-no-alg parseBearer export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1229",
		label: "grid verify-no-alg parseBearer async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1230",
		label: "grid verify-no-alg parseBearer iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1231",
		label: "grid verify-alg parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1232",
		label: "grid verify-alg parseBearer module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1233",
		label: "grid verify-alg parseBearer export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1234",
		label: "grid verify-alg parseBearer async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1235",
		label: "grid verify-alg parseBearer iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1236",
		label: "grid sign-no-exp parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1237",
		label: "grid sign-no-exp parseBearer module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1238",
		label: "grid sign-no-exp parseBearer export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1239",
		label: "grid sign-no-exp parseBearer async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1240",
		label: "grid sign-no-exp parseBearer iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1241",
		label: "grid decode validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1242",
		label: "grid decode validateSession module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1243",
		label: "grid decode validateSession export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1244",
		label: "grid decode validateSession async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1245",
		label: "grid decode validateSession iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1246",
		label: "grid verify-no-alg validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1247",
		label: "grid verify-no-alg validateSession module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1248",
		label: "grid verify-no-alg validateSession export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1249",
		label: "grid verify-no-alg validateSession async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1250",
		label: "grid verify-no-alg validateSession iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1251",
		label: "grid verify-alg validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1252",
		label: "grid verify-alg validateSession module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1253",
		label: "grid verify-alg validateSession export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1254",
		label: "grid verify-alg validateSession async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1255",
		label: "grid verify-alg validateSession iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1256",
		label: "grid sign-no-exp validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1257",
		label: "grid sign-no-exp validateSession module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1258",
		label: "grid sign-no-exp validateSession export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1259",
		label: "grid sign-no-exp validateSession async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1260",
		label: "grid sign-no-exp validateSession iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1261",
		label: "grid decode loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1262",
		label: "grid decode loginUser module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1263",
		label: "grid decode loginUser export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1264",
		label: "grid decode loginUser async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1265",
		label: "grid decode loginUser iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1266",
		label: "grid verify-no-alg loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1267",
		label: "grid verify-no-alg loginUser module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1268",
		label: "grid verify-no-alg loginUser export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1269",
		label: "grid verify-no-alg loginUser async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1270",
		label: "grid verify-no-alg loginUser iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1271",
		label: "grid verify-alg loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1272",
		label: "grid verify-alg loginUser module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1273",
		label: "grid verify-alg loginUser export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1274",
		label: "grid verify-alg loginUser async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1275",
		label: "grid verify-alg loginUser iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1276",
		label: "grid sign-no-exp loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1277",
		label: "grid sign-no-exp loginUser module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1278",
		label: "grid sign-no-exp loginUser export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1279",
		label: "grid sign-no-exp loginUser async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1280",
		label: "grid sign-no-exp loginUser iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1281",
		label: "grid decode middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1282",
		label: "grid decode middleware module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1283",
		label: "grid decode middleware export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1284",
		label: "grid decode middleware async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1285",
		label: "grid decode middleware iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-01": 1,
		},
	},
	{
		id: "1286",
		label: "grid verify-no-alg middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1287",
		label: "grid verify-no-alg middleware module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1288",
		label: "grid verify-no-alg middleware export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1289",
		label: "grid verify-no-alg middleware async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1290",
		label: "grid verify-no-alg middleware iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1291",
		label: "grid verify-alg middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1292",
		label: "grid verify-alg middleware module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1293",
		label: "grid verify-alg middleware export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1294",
		label: "grid verify-alg middleware async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1295",
		label: "grid verify-alg middleware iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1296",
		label: "grid sign-no-exp middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1297",
		label: "grid sign-no-exp middleware module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1298",
		label: "grid sign-no-exp middleware export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1299",
		label: "grid sign-no-exp middleware async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1300",
		label: "grid sign-no-exp middleware iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-05": 1,
		},
	},
	{
		id: "1301",
		label: "ext jwt03 none authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1302",
		label: "ext jwt03 none authenticate module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1303",
		label: "ext jwt03 none authenticate export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1304",
		label: "ext jwt03 none authenticate async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1305",
		label: "ext jwt03 none authenticate iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1306",
		label: "ext jwt04 ignoreExp authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1307",
		label: "ext jwt04 ignoreExp authenticate module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1308",
		label: "ext jwt04 ignoreExp authenticate export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1309",
		label: "ext jwt04 ignoreExp authenticate async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1310",
		label: "ext jwt04 ignoreExp authenticate iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1311",
		label: "ext jwt06 noTimestamp authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1312",
		label: "ext jwt06 noTimestamp authenticate module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1313",
		label: "ext jwt06 noTimestamp authenticate export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1314",
		label: "ext jwt06 noTimestamp authenticate async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1315",
		label: "ext jwt06 noTimestamp authenticate iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1316",
		label: "ext jwt02 spread alg authenticate use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1317",
		label: "ext jwt02 spread alg authenticate module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1318",
		label: "ext jwt02 spread alg authenticate export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1319",
		label: "ext jwt02 spread alg authenticate async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1320",
		label: "ext jwt02 spread alg authenticate iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1321",
		label: "ext jwt03 none handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1322",
		label: "ext jwt03 none handleAuth module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1323",
		label: "ext jwt03 none handleAuth export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1324",
		label: "ext jwt03 none handleAuth async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1325",
		label: "ext jwt03 none handleAuth iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1326",
		label: "ext jwt04 ignoreExp handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1327",
		label: "ext jwt04 ignoreExp handleAuth module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1328",
		label: "ext jwt04 ignoreExp handleAuth export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1329",
		label: "ext jwt04 ignoreExp handleAuth async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1330",
		label: "ext jwt04 ignoreExp handleAuth iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1331",
		label: "ext jwt06 noTimestamp handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1332",
		label: "ext jwt06 noTimestamp handleAuth module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1333",
		label: "ext jwt06 noTimestamp handleAuth export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1334",
		label: "ext jwt06 noTimestamp handleAuth async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1335",
		label: "ext jwt06 noTimestamp handleAuth iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1336",
		label: "ext jwt02 spread alg handleAuth use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1337",
		label: "ext jwt02 spread alg handleAuth module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1338",
		label: "ext jwt02 spread alg handleAuth export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1339",
		label: "ext jwt02 spread alg handleAuth async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1340",
		label: "ext jwt02 spread alg handleAuth iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1341",
		label: "ext jwt03 none verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1342",
		label: "ext jwt03 none verifyRequest module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1343",
		label: "ext jwt03 none verifyRequest export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1344",
		label: "ext jwt03 none verifyRequest async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1345",
		label: "ext jwt03 none verifyRequest iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1346",
		label: "ext jwt04 ignoreExp verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1347",
		label: "ext jwt04 ignoreExp verifyRequest module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1348",
		label: "ext jwt04 ignoreExp verifyRequest export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1349",
		label: "ext jwt04 ignoreExp verifyRequest async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1350",
		label: "ext jwt04 ignoreExp verifyRequest iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1351",
		label: "ext jwt06 noTimestamp verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1352",
		label: "ext jwt06 noTimestamp verifyRequest module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1353",
		label: "ext jwt06 noTimestamp verifyRequest export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1354",
		label: "ext jwt06 noTimestamp verifyRequest async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1355",
		label: "ext jwt06 noTimestamp verifyRequest iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1356",
		label: "ext jwt02 spread alg verifyRequest use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1357",
		label: "ext jwt02 spread alg verifyRequest module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1358",
		label: "ext jwt02 spread alg verifyRequest export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1359",
		label: "ext jwt02 spread alg verifyRequest async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1360",
		label: "ext jwt02 spread alg verifyRequest iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1361",
		label: "ext jwt03 none issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1362",
		label: "ext jwt03 none issueCredentials module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1363",
		label: "ext jwt03 none issueCredentials export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1364",
		label: "ext jwt03 none issueCredentials async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1365",
		label: "ext jwt03 none issueCredentials iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1366",
		label: "ext jwt04 ignoreExp issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1367",
		label: "ext jwt04 ignoreExp issueCredentials module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1368",
		label: "ext jwt04 ignoreExp issueCredentials export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1369",
		label: "ext jwt04 ignoreExp issueCredentials async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1370",
		label: "ext jwt04 ignoreExp issueCredentials iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1371",
		label: "ext jwt06 noTimestamp issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1372",
		label: "ext jwt06 noTimestamp issueCredentials module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1373",
		label: "ext jwt06 noTimestamp issueCredentials export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1374",
		label: "ext jwt06 noTimestamp issueCredentials async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1375",
		label: "ext jwt06 noTimestamp issueCredentials iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1376",
		label: "ext jwt02 spread alg issueCredentials use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1377",
		label: "ext jwt02 spread alg issueCredentials module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1378",
		label: "ext jwt02 spread alg issueCredentials export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1379",
		label: "ext jwt02 spread alg issueCredentials async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1380",
		label: "ext jwt02 spread alg issueCredentials iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1381",
		label: "ext jwt03 none parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1382",
		label: "ext jwt03 none parseBearer module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1383",
		label: "ext jwt03 none parseBearer export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1384",
		label: "ext jwt03 none parseBearer async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1385",
		label: "ext jwt03 none parseBearer iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1386",
		label: "ext jwt04 ignoreExp parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1387",
		label: "ext jwt04 ignoreExp parseBearer module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1388",
		label: "ext jwt04 ignoreExp parseBearer export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1389",
		label: "ext jwt04 ignoreExp parseBearer async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1390",
		label: "ext jwt04 ignoreExp parseBearer iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1391",
		label: "ext jwt06 noTimestamp parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1392",
		label: "ext jwt06 noTimestamp parseBearer module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1393",
		label: "ext jwt06 noTimestamp parseBearer export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1394",
		label: "ext jwt06 noTimestamp parseBearer async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1395",
		label: "ext jwt06 noTimestamp parseBearer iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1396",
		label: "ext jwt02 spread alg parseBearer use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1397",
		label: "ext jwt02 spread alg parseBearer module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1398",
		label: "ext jwt02 spread alg parseBearer export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1399",
		label: "ext jwt02 spread alg parseBearer async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1400",
		label: "ext jwt02 spread alg parseBearer iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1401",
		label: "ext jwt03 none validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1402",
		label: "ext jwt03 none validateSession module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1403",
		label: "ext jwt03 none validateSession export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1404",
		label: "ext jwt03 none validateSession async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1405",
		label: "ext jwt03 none validateSession iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1406",
		label: "ext jwt04 ignoreExp validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1407",
		label: "ext jwt04 ignoreExp validateSession module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1408",
		label: "ext jwt04 ignoreExp validateSession export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1409",
		label: "ext jwt04 ignoreExp validateSession async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1410",
		label: "ext jwt04 ignoreExp validateSession iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1411",
		label: "ext jwt06 noTimestamp validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1412",
		label: "ext jwt06 noTimestamp validateSession module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1413",
		label: "ext jwt06 noTimestamp validateSession export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1414",
		label: "ext jwt06 noTimestamp validateSession async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1415",
		label: "ext jwt06 noTimestamp validateSession iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1416",
		label: "ext jwt02 spread alg validateSession use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1417",
		label: "ext jwt02 spread alg validateSession module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1418",
		label: "ext jwt02 spread alg validateSession export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1419",
		label: "ext jwt02 spread alg validateSession async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1420",
		label: "ext jwt02 spread alg validateSession iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1421",
		label: "ext jwt03 none loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1422",
		label: "ext jwt03 none loginUser module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1423",
		label: "ext jwt03 none loginUser export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1424",
		label: "ext jwt03 none loginUser async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1425",
		label: "ext jwt03 none loginUser iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1426",
		label: "ext jwt04 ignoreExp loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1427",
		label: "ext jwt04 ignoreExp loginUser module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1428",
		label: "ext jwt04 ignoreExp loginUser export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1429",
		label: "ext jwt04 ignoreExp loginUser async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1430",
		label: "ext jwt04 ignoreExp loginUser iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1431",
		label: "ext jwt06 noTimestamp loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1432",
		label: "ext jwt06 noTimestamp loginUser module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1433",
		label: "ext jwt06 noTimestamp loginUser export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1434",
		label: "ext jwt06 noTimestamp loginUser async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1435",
		label: "ext jwt06 noTimestamp loginUser iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1436",
		label: "ext jwt02 spread alg loginUser use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1437",
		label: "ext jwt02 spread alg loginUser module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1438",
		label: "ext jwt02 spread alg loginUser export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1439",
		label: "ext jwt02 spread alg loginUser async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1440",
		label: "ext jwt02 spread alg loginUser iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1441",
		label: "ext jwt03 none middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1442",
		label: "ext jwt03 none middleware module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1443",
		label: "ext jwt03 none middleware export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1444",
		label: "ext jwt03 none middleware async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1445",
		label: "ext jwt03 none middleware iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1446",
		label: "ext jwt04 ignoreExp middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1447",
		label: "ext jwt04 ignoreExp middleware module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1448",
		label: "ext jwt04 ignoreExp middleware export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1449",
		label: "ext jwt04 ignoreExp middleware async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1450",
		label: "ext jwt04 ignoreExp middleware iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1451",
		label: "ext jwt06 noTimestamp middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1452",
		label: "ext jwt06 noTimestamp middleware module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1453",
		label: "ext jwt06 noTimestamp middleware export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1454",
		label: "ext jwt06 noTimestamp middleware async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1455",
		label: "ext jwt06 noTimestamp middleware iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1456",
		label: "ext jwt02 spread alg middleware use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1457",
		label: "ext jwt02 spread alg middleware module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1458",
		label: "ext jwt02 spread alg middleware export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1459",
		label: "ext jwt02 spread alg middleware async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1460",
		label: "ext jwt02 spread alg middleware iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1461",
		label: "ext jwt03 none refreshToken use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1462",
		label: "ext jwt03 none refreshToken module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1463",
		label: "ext jwt03 none refreshToken export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1464",
		label: "ext jwt03 none refreshToken async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1465",
		label: "ext jwt03 none refreshToken iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1466",
		label: "ext jwt04 ignoreExp refreshToken use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1467",
		label: "ext jwt04 ignoreExp refreshToken module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1468",
		label: "ext jwt04 ignoreExp refreshToken export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1469",
		label: "ext jwt04 ignoreExp refreshToken async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1470",
		label: "ext jwt04 ignoreExp refreshToken iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1471",
		label: "ext jwt06 noTimestamp refreshToken use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1472",
		label: "ext jwt06 noTimestamp refreshToken module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1473",
		label: "ext jwt06 noTimestamp refreshToken export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1474",
		label: "ext jwt06 noTimestamp refreshToken async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1475",
		label: "ext jwt06 noTimestamp refreshToken iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1476",
		label: "ext jwt02 spread alg refreshToken use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1477",
		label: "ext jwt02 spread alg refreshToken module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1478",
		label: "ext jwt02 spread alg refreshToken export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1479",
		label: "ext jwt02 spread alg refreshToken async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1480",
		label: "ext jwt02 spread alg refreshToken iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1481",
		label: "ext jwt03 none decodeClaims use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1482",
		label: "ext jwt03 none decodeClaims module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1483",
		label: "ext jwt03 none decodeClaims export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1484",
		label: "ext jwt03 none decodeClaims async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1485",
		label: "ext jwt03 none decodeClaims iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1486",
		label: "ext jwt04 ignoreExp decodeClaims use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1487",
		label: "ext jwt04 ignoreExp decodeClaims module banner",
		source:
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1488",
		label: "ext jwt04 ignoreExp decodeClaims export const",
		source:
			'import jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1489",
		label: "ext jwt04 ignoreExp decodeClaims async export",
		source:
			'import jwt from "jsonwebtoken";\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1490",
		label: "ext jwt04 ignoreExp decodeClaims iife suffix",
		source:
			'import jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1491",
		label: "ext jwt06 noTimestamp decodeClaims use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1492",
		label: "ext jwt06 noTimestamp decodeClaims module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1493",
		label: "ext jwt06 noTimestamp decodeClaims export const",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1494",
		label: "ext jwt06 noTimestamp decodeClaims async export",
		source:
			"import jwt from \"jsonwebtoken\";\nexport async function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1495",
		label: "ext jwt06 noTimestamp decodeClaims iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1496",
		label: "ext jwt02 spread alg decodeClaims use strict",
		source:
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1497",
		label: "ext jwt02 spread alg decodeClaims module banner",
		source:
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1498",
		label: "ext jwt02 spread alg decodeClaims export const",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1499",
		label: "ext jwt02 spread alg decodeClaims async export",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1500",
		label: "ext jwt02 spread alg decodeClaims iife suffix",
		source:
			"import jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1501",
		label: "ext jwt03 none authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1502",
		label: "ext jwt03 none authenticate module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1503",
		label: "ext jwt03 none authenticate export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1504",
		label: "ext jwt03 none authenticate async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1505",
		label: "ext jwt03 none authenticate iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1506",
		label: "ext jwt04 ignoreExp authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1507",
		label: "ext jwt04 ignoreExp authenticate module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1508",
		label: "ext jwt04 ignoreExp authenticate export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1509",
		label: "ext jwt04 ignoreExp authenticate async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1510",
		label: "ext jwt04 ignoreExp authenticate iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1511",
		label: "ext jwt06 noTimestamp authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1512",
		label: "ext jwt06 noTimestamp authenticate module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1513",
		label: "ext jwt06 noTimestamp authenticate export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1514",
		label: "ext jwt06 noTimestamp authenticate async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1515",
		label: "ext jwt06 noTimestamp authenticate iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1516",
		label: "ext jwt02 spread alg authenticate use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1517",
		label: "ext jwt02 spread alg authenticate module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1518",
		label: "ext jwt02 spread alg authenticate export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1519",
		label: "ext jwt02 spread alg authenticate async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1520",
		label: "ext jwt02 spread alg authenticate iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1521",
		label: "ext jwt03 none handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1522",
		label: "ext jwt03 none handleAuth module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1523",
		label: "ext jwt03 none handleAuth export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1524",
		label: "ext jwt03 none handleAuth async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1525",
		label: "ext jwt03 none handleAuth iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1526",
		label: "ext jwt04 ignoreExp handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1527",
		label: "ext jwt04 ignoreExp handleAuth module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1528",
		label: "ext jwt04 ignoreExp handleAuth export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1529",
		label: "ext jwt04 ignoreExp handleAuth async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1530",
		label: "ext jwt04 ignoreExp handleAuth iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1531",
		label: "ext jwt06 noTimestamp handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1532",
		label: "ext jwt06 noTimestamp handleAuth module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1533",
		label: "ext jwt06 noTimestamp handleAuth export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1534",
		label: "ext jwt06 noTimestamp handleAuth async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1535",
		label: "ext jwt06 noTimestamp handleAuth iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1536",
		label: "ext jwt02 spread alg handleAuth use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1537",
		label: "ext jwt02 spread alg handleAuth module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1538",
		label: "ext jwt02 spread alg handleAuth export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1539",
		label: "ext jwt02 spread alg handleAuth async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1540",
		label: "ext jwt02 spread alg handleAuth iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1541",
		label: "ext jwt03 none verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1542",
		label: "ext jwt03 none verifyRequest module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1543",
		label: "ext jwt03 none verifyRequest export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1544",
		label: "ext jwt03 none verifyRequest async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1545",
		label: "ext jwt03 none verifyRequest iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1546",
		label: "ext jwt04 ignoreExp verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1547",
		label: "ext jwt04 ignoreExp verifyRequest module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1548",
		label: "ext jwt04 ignoreExp verifyRequest export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1549",
		label: "ext jwt04 ignoreExp verifyRequest async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1550",
		label: "ext jwt04 ignoreExp verifyRequest iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1551",
		label: "ext jwt06 noTimestamp verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1552",
		label: "ext jwt06 noTimestamp verifyRequest module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1553",
		label: "ext jwt06 noTimestamp verifyRequest export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1554",
		label: "ext jwt06 noTimestamp verifyRequest async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1555",
		label: "ext jwt06 noTimestamp verifyRequest iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1556",
		label: "ext jwt02 spread alg verifyRequest use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1557",
		label: "ext jwt02 spread alg verifyRequest module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1558",
		label: "ext jwt02 spread alg verifyRequest export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1559",
		label: "ext jwt02 spread alg verifyRequest async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1560",
		label: "ext jwt02 spread alg verifyRequest iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1561",
		label: "ext jwt03 none issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1562",
		label: "ext jwt03 none issueCredentials module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1563",
		label: "ext jwt03 none issueCredentials export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1564",
		label: "ext jwt03 none issueCredentials async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1565",
		label: "ext jwt03 none issueCredentials iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1566",
		label: "ext jwt04 ignoreExp issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1567",
		label: "ext jwt04 ignoreExp issueCredentials module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1568",
		label: "ext jwt04 ignoreExp issueCredentials export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1569",
		label: "ext jwt04 ignoreExp issueCredentials async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1570",
		label: "ext jwt04 ignoreExp issueCredentials iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1571",
		label: "ext jwt06 noTimestamp issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1572",
		label: "ext jwt06 noTimestamp issueCredentials module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1573",
		label: "ext jwt06 noTimestamp issueCredentials export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1574",
		label: "ext jwt06 noTimestamp issueCredentials async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1575",
		label: "ext jwt06 noTimestamp issueCredentials iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1576",
		label: "ext jwt02 spread alg issueCredentials use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1577",
		label: "ext jwt02 spread alg issueCredentials module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1578",
		label: "ext jwt02 spread alg issueCredentials export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1579",
		label: "ext jwt02 spread alg issueCredentials async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1580",
		label: "ext jwt02 spread alg issueCredentials iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1581",
		label: "ext jwt03 none parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1582",
		label: "ext jwt03 none parseBearer module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1583",
		label: "ext jwt03 none parseBearer export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1584",
		label: "ext jwt03 none parseBearer async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1585",
		label: "ext jwt03 none parseBearer iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1586",
		label: "ext jwt04 ignoreExp parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1587",
		label: "ext jwt04 ignoreExp parseBearer module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1588",
		label: "ext jwt04 ignoreExp parseBearer export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1589",
		label: "ext jwt04 ignoreExp parseBearer async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1590",
		label: "ext jwt04 ignoreExp parseBearer iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1591",
		label: "ext jwt06 noTimestamp parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1592",
		label: "ext jwt06 noTimestamp parseBearer module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1593",
		label: "ext jwt06 noTimestamp parseBearer export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1594",
		label: "ext jwt06 noTimestamp parseBearer async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1595",
		label: "ext jwt06 noTimestamp parseBearer iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1596",
		label: "ext jwt02 spread alg parseBearer use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1597",
		label: "ext jwt02 spread alg parseBearer module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1598",
		label: "ext jwt02 spread alg parseBearer export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1599",
		label: "ext jwt02 spread alg parseBearer async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1600",
		label: "ext jwt02 spread alg parseBearer iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1601",
		label: "ext jwt03 none validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1602",
		label: "ext jwt03 none validateSession module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1603",
		label: "ext jwt03 none validateSession export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1604",
		label: "ext jwt03 none validateSession async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1605",
		label: "ext jwt03 none validateSession iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1606",
		label: "ext jwt04 ignoreExp validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1607",
		label: "ext jwt04 ignoreExp validateSession module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1608",
		label: "ext jwt04 ignoreExp validateSession export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1609",
		label: "ext jwt04 ignoreExp validateSession async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1610",
		label: "ext jwt04 ignoreExp validateSession iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1611",
		label: "ext jwt06 noTimestamp validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1612",
		label: "ext jwt06 noTimestamp validateSession module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1613",
		label: "ext jwt06 noTimestamp validateSession export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1614",
		label: "ext jwt06 noTimestamp validateSession async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1615",
		label: "ext jwt06 noTimestamp validateSession iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1616",
		label: "ext jwt02 spread alg validateSession use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1617",
		label: "ext jwt02 spread alg validateSession module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1618",
		label: "ext jwt02 spread alg validateSession export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1619",
		label: "ext jwt02 spread alg validateSession async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1620",
		label: "ext jwt02 spread alg validateSession iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1621",
		label: "ext jwt03 none loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1622",
		label: "ext jwt03 none loginUser module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1623",
		label: "ext jwt03 none loginUser export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1624",
		label: "ext jwt03 none loginUser async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1625",
		label: "ext jwt03 none loginUser iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1626",
		label: "ext jwt04 ignoreExp loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1627",
		label: "ext jwt04 ignoreExp loginUser module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1628",
		label: "ext jwt04 ignoreExp loginUser export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1629",
		label: "ext jwt04 ignoreExp loginUser async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1630",
		label: "ext jwt04 ignoreExp loginUser iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1631",
		label: "ext jwt06 noTimestamp loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1632",
		label: "ext jwt06 noTimestamp loginUser module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1633",
		label: "ext jwt06 noTimestamp loginUser export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1634",
		label: "ext jwt06 noTimestamp loginUser async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1635",
		label: "ext jwt06 noTimestamp loginUser iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1636",
		label: "ext jwt02 spread alg loginUser use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1637",
		label: "ext jwt02 spread alg loginUser module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1638",
		label: "ext jwt02 spread alg loginUser export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1639",
		label: "ext jwt02 spread alg loginUser async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1640",
		label: "ext jwt02 spread alg loginUser iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1641",
		label: "ext jwt03 none middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1642",
		label: "ext jwt03 none middleware module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1643",
		label: "ext jwt03 none middleware export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1644",
		label: "ext jwt03 none middleware async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1645",
		label: "ext jwt03 none middleware iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1646",
		label: "ext jwt04 ignoreExp middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1647",
		label: "ext jwt04 ignoreExp middleware module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1648",
		label: "ext jwt04 ignoreExp middleware export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1649",
		label: "ext jwt04 ignoreExp middleware async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1650",
		label: "ext jwt04 ignoreExp middleware iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1651",
		label: "ext jwt06 noTimestamp middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1652",
		label: "ext jwt06 noTimestamp middleware module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1653",
		label: "ext jwt06 noTimestamp middleware export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1654",
		label: "ext jwt06 noTimestamp middleware async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1655",
		label: "ext jwt06 noTimestamp middleware iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1656",
		label: "ext jwt02 spread alg middleware use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1657",
		label: "ext jwt02 spread alg middleware module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1658",
		label: "ext jwt02 spread alg middleware export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1659",
		label: "ext jwt02 spread alg middleware async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1660",
		label: "ext jwt02 spread alg middleware iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1661",
		label: "ext jwt03 none refreshToken use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1662",
		label: "ext jwt03 none refreshToken module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1663",
		label: "ext jwt03 none refreshToken export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1664",
		label: "ext jwt03 none refreshToken async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1665",
		label: "ext jwt03 none refreshToken iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1666",
		label: "ext jwt04 ignoreExp refreshToken use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1667",
		label: "ext jwt04 ignoreExp refreshToken module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1668",
		label: "ext jwt04 ignoreExp refreshToken export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1669",
		label: "ext jwt04 ignoreExp refreshToken async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1670",
		label: "ext jwt04 ignoreExp refreshToken iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1671",
		label: "ext jwt06 noTimestamp refreshToken use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1672",
		label: "ext jwt06 noTimestamp refreshToken module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1673",
		label: "ext jwt06 noTimestamp refreshToken export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1674",
		label: "ext jwt06 noTimestamp refreshToken async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1675",
		label: "ext jwt06 noTimestamp refreshToken iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1676",
		label: "ext jwt02 spread alg refreshToken use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1677",
		label: "ext jwt02 spread alg refreshToken module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1678",
		label: "ext jwt02 spread alg refreshToken export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1679",
		label: "ext jwt02 spread alg refreshToken async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1680",
		label: "ext jwt02 spread alg refreshToken iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1681",
		label: "ext jwt03 none decodeClaims use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1682",
		label: "ext jwt03 none decodeClaims module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1683",
		label: "ext jwt03 none decodeClaims export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1684",
		label: "ext jwt03 none decodeClaims async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1685",
		label: "ext jwt03 none decodeClaims iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1686",
		label: "ext jwt04 ignoreExp decodeClaims use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1687",
		label: "ext jwt04 ignoreExp decodeClaims module banner",
		source:
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1688",
		label: "ext jwt04 ignoreExp decodeClaims export const",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1689",
		label: "ext jwt04 ignoreExp decodeClaims async export",
		source:
			'import * as jwt from "jsonwebtoken";\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1690",
		label: "ext jwt04 ignoreExp decodeClaims iife suffix",
		source:
			'import * as jwt from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1691",
		label: "ext jwt06 noTimestamp decodeClaims use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1692",
		label: "ext jwt06 noTimestamp decodeClaims module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1693",
		label: "ext jwt06 noTimestamp decodeClaims export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1694",
		label: "ext jwt06 noTimestamp decodeClaims async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport async function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1695",
		label: "ext jwt06 noTimestamp decodeClaims iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1696",
		label: "ext jwt02 spread alg decodeClaims use strict",
		source:
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1697",
		label: "ext jwt02 spread alg decodeClaims module banner",
		source:
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1698",
		label: "ext jwt02 spread alg decodeClaims export const",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1699",
		label: "ext jwt02 spread alg decodeClaims async export",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1700",
		label: "ext jwt02 spread alg decodeClaims iife suffix",
		source:
			"import * as jwt from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1701",
		label: "ext jwt03 none authenticate use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1702",
		label: "ext jwt03 none authenticate module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1703",
		label: "ext jwt03 none authenticate export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1704",
		label: "ext jwt03 none authenticate async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1705",
		label: "ext jwt03 none authenticate iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1706",
		label: "ext jwt04 ignoreExp authenticate use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1707",
		label: "ext jwt04 ignoreExp authenticate module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1708",
		label: "ext jwt04 ignoreExp authenticate export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1709",
		label: "ext jwt04 ignoreExp authenticate async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1710",
		label: "ext jwt04 ignoreExp authenticate iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1711",
		label: "ext jwt06 noTimestamp authenticate use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1712",
		label: "ext jwt06 noTimestamp authenticate module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1713",
		label: "ext jwt06 noTimestamp authenticate export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1714",
		label: "ext jwt06 noTimestamp authenticate async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function authenticate(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1715",
		label: "ext jwt06 noTimestamp authenticate iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function authenticate(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1716",
		label: "ext jwt02 spread alg authenticate use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1717",
		label: "ext jwt02 spread alg authenticate module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1718",
		label: "ext jwt02 spread alg authenticate export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1719",
		label: "ext jwt02 spread alg authenticate async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1720",
		label: "ext jwt02 spread alg authenticate iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1721",
		label: "ext jwt03 none handleAuth use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1722",
		label: "ext jwt03 none handleAuth module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1723",
		label: "ext jwt03 none handleAuth export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1724",
		label: "ext jwt03 none handleAuth async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1725",
		label: "ext jwt03 none handleAuth iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1726",
		label: "ext jwt04 ignoreExp handleAuth use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1727",
		label: "ext jwt04 ignoreExp handleAuth module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1728",
		label: "ext jwt04 ignoreExp handleAuth export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1729",
		label: "ext jwt04 ignoreExp handleAuth async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1730",
		label: "ext jwt04 ignoreExp handleAuth iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1731",
		label: "ext jwt06 noTimestamp handleAuth use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1732",
		label: "ext jwt06 noTimestamp handleAuth module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1733",
		label: "ext jwt06 noTimestamp handleAuth export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1734",
		label: "ext jwt06 noTimestamp handleAuth async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function handleAuth(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1735",
		label: "ext jwt06 noTimestamp handleAuth iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1736",
		label: "ext jwt02 spread alg handleAuth use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1737",
		label: "ext jwt02 spread alg handleAuth module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1738",
		label: "ext jwt02 spread alg handleAuth export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1739",
		label: "ext jwt02 spread alg handleAuth async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1740",
		label: "ext jwt02 spread alg handleAuth iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1741",
		label: "ext jwt03 none verifyRequest use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1742",
		label: "ext jwt03 none verifyRequest module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1743",
		label: "ext jwt03 none verifyRequest export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1744",
		label: "ext jwt03 none verifyRequest async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1745",
		label: "ext jwt03 none verifyRequest iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1746",
		label: "ext jwt04 ignoreExp verifyRequest use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1747",
		label: "ext jwt04 ignoreExp verifyRequest module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1748",
		label: "ext jwt04 ignoreExp verifyRequest export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1749",
		label: "ext jwt04 ignoreExp verifyRequest async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1750",
		label: "ext jwt04 ignoreExp verifyRequest iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1751",
		label: "ext jwt06 noTimestamp verifyRequest use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1752",
		label: "ext jwt06 noTimestamp verifyRequest module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1753",
		label: "ext jwt06 noTimestamp verifyRequest export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1754",
		label: "ext jwt06 noTimestamp verifyRequest async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function verifyRequest(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1755",
		label: "ext jwt06 noTimestamp verifyRequest iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1756",
		label: "ext jwt02 spread alg verifyRequest use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1757",
		label: "ext jwt02 spread alg verifyRequest module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1758",
		label: "ext jwt02 spread alg verifyRequest export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1759",
		label: "ext jwt02 spread alg verifyRequest async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1760",
		label: "ext jwt02 spread alg verifyRequest iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1761",
		label: "ext jwt03 none issueCredentials use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1762",
		label: "ext jwt03 none issueCredentials module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1763",
		label: "ext jwt03 none issueCredentials export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1764",
		label: "ext jwt03 none issueCredentials async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1765",
		label: "ext jwt03 none issueCredentials iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1766",
		label: "ext jwt04 ignoreExp issueCredentials use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1767",
		label: "ext jwt04 ignoreExp issueCredentials module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1768",
		label: "ext jwt04 ignoreExp issueCredentials export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1769",
		label: "ext jwt04 ignoreExp issueCredentials async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1770",
		label: "ext jwt04 ignoreExp issueCredentials iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1771",
		label: "ext jwt06 noTimestamp issueCredentials use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1772",
		label: "ext jwt06 noTimestamp issueCredentials module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1773",
		label: "ext jwt06 noTimestamp issueCredentials export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1774",
		label: "ext jwt06 noTimestamp issueCredentials async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function issueCredentials(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1775",
		label: "ext jwt06 noTimestamp issueCredentials iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1776",
		label: "ext jwt02 spread alg issueCredentials use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1777",
		label: "ext jwt02 spread alg issueCredentials module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1778",
		label: "ext jwt02 spread alg issueCredentials export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1779",
		label: "ext jwt02 spread alg issueCredentials async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1780",
		label: "ext jwt02 spread alg issueCredentials iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1781",
		label: "ext jwt03 none parseBearer use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1782",
		label: "ext jwt03 none parseBearer module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1783",
		label: "ext jwt03 none parseBearer export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1784",
		label: "ext jwt03 none parseBearer async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1785",
		label: "ext jwt03 none parseBearer iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1786",
		label: "ext jwt04 ignoreExp parseBearer use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1787",
		label: "ext jwt04 ignoreExp parseBearer module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1788",
		label: "ext jwt04 ignoreExp parseBearer export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1789",
		label: "ext jwt04 ignoreExp parseBearer async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1790",
		label: "ext jwt04 ignoreExp parseBearer iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1791",
		label: "ext jwt06 noTimestamp parseBearer use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1792",
		label: "ext jwt06 noTimestamp parseBearer module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1793",
		label: "ext jwt06 noTimestamp parseBearer export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1794",
		label: "ext jwt06 noTimestamp parseBearer async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function parseBearer(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1795",
		label: "ext jwt06 noTimestamp parseBearer iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1796",
		label: "ext jwt02 spread alg parseBearer use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1797",
		label: "ext jwt02 spread alg parseBearer module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1798",
		label: "ext jwt02 spread alg parseBearer export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1799",
		label: "ext jwt02 spread alg parseBearer async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1800",
		label: "ext jwt02 spread alg parseBearer iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1801",
		label: "ext jwt03 none validateSession use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1802",
		label: "ext jwt03 none validateSession module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1803",
		label: "ext jwt03 none validateSession export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1804",
		label: "ext jwt03 none validateSession async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1805",
		label: "ext jwt03 none validateSession iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1806",
		label: "ext jwt04 ignoreExp validateSession use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1807",
		label: "ext jwt04 ignoreExp validateSession module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1808",
		label: "ext jwt04 ignoreExp validateSession export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1809",
		label: "ext jwt04 ignoreExp validateSession async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1810",
		label: "ext jwt04 ignoreExp validateSession iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1811",
		label: "ext jwt06 noTimestamp validateSession use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1812",
		label: "ext jwt06 noTimestamp validateSession module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1813",
		label: "ext jwt06 noTimestamp validateSession export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1814",
		label: "ext jwt06 noTimestamp validateSession async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function validateSession(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1815",
		label: "ext jwt06 noTimestamp validateSession iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function validateSession(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1816",
		label: "ext jwt02 spread alg validateSession use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1817",
		label: "ext jwt02 spread alg validateSession module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1818",
		label: "ext jwt02 spread alg validateSession export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1819",
		label: "ext jwt02 spread alg validateSession async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1820",
		label: "ext jwt02 spread alg validateSession iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1821",
		label: "ext jwt03 none loginUser use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1822",
		label: "ext jwt03 none loginUser module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1823",
		label: "ext jwt03 none loginUser export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1824",
		label: "ext jwt03 none loginUser async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1825",
		label: "ext jwt03 none loginUser iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1826",
		label: "ext jwt04 ignoreExp loginUser use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1827",
		label: "ext jwt04 ignoreExp loginUser module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1828",
		label: "ext jwt04 ignoreExp loginUser export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1829",
		label: "ext jwt04 ignoreExp loginUser async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1830",
		label: "ext jwt04 ignoreExp loginUser iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1831",
		label: "ext jwt06 noTimestamp loginUser use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1832",
		label: "ext jwt06 noTimestamp loginUser module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1833",
		label: "ext jwt06 noTimestamp loginUser export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1834",
		label: "ext jwt06 noTimestamp loginUser async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function loginUser(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1835",
		label: "ext jwt06 noTimestamp loginUser iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function loginUser(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1836",
		label: "ext jwt02 spread alg loginUser use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1837",
		label: "ext jwt02 spread alg loginUser module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1838",
		label: "ext jwt02 spread alg loginUser export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1839",
		label: "ext jwt02 spread alg loginUser async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1840",
		label: "ext jwt02 spread alg loginUser iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1841",
		label: "ext jwt03 none middleware use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1842",
		label: "ext jwt03 none middleware module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1843",
		label: "ext jwt03 none middleware export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1844",
		label: "ext jwt03 none middleware async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1845",
		label: "ext jwt03 none middleware iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1846",
		label: "ext jwt04 ignoreExp middleware use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1847",
		label: "ext jwt04 ignoreExp middleware module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1848",
		label: "ext jwt04 ignoreExp middleware export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1849",
		label: "ext jwt04 ignoreExp middleware async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1850",
		label: "ext jwt04 ignoreExp middleware iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1851",
		label: "ext jwt06 noTimestamp middleware use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1852",
		label: "ext jwt06 noTimestamp middleware module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1853",
		label: "ext jwt06 noTimestamp middleware export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1854",
		label: "ext jwt06 noTimestamp middleware async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function middleware(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1855",
		label: "ext jwt06 noTimestamp middleware iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function middleware(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1856",
		label: "ext jwt02 spread alg middleware use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1857",
		label: "ext jwt02 spread alg middleware module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1858",
		label: "ext jwt02 spread alg middleware export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1859",
		label: "ext jwt02 spread alg middleware async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1860",
		label: "ext jwt02 spread alg middleware iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1861",
		label: "ext jwt03 none refreshToken use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1862",
		label: "ext jwt03 none refreshToken module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1863",
		label: "ext jwt03 none refreshToken export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1864",
		label: "ext jwt03 none refreshToken async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1865",
		label: "ext jwt03 none refreshToken iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1866",
		label: "ext jwt04 ignoreExp refreshToken use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1867",
		label: "ext jwt04 ignoreExp refreshToken module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1868",
		label: "ext jwt04 ignoreExp refreshToken export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1869",
		label: "ext jwt04 ignoreExp refreshToken async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1870",
		label: "ext jwt04 ignoreExp refreshToken iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1871",
		label: "ext jwt06 noTimestamp refreshToken use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function refreshToken(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1872",
		label: "ext jwt06 noTimestamp refreshToken module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1873",
		label: "ext jwt06 noTimestamp refreshToken export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1874",
		label: "ext jwt06 noTimestamp refreshToken async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function refreshToken(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1875",
		label: "ext jwt06 noTimestamp refreshToken iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function refreshToken(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1876",
		label: "ext jwt02 spread alg refreshToken use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1877",
		label: "ext jwt02 spread alg refreshToken module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1878",
		label: "ext jwt02 spread alg refreshToken export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1879",
		label: "ext jwt02 spread alg refreshToken async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1880",
		label: "ext jwt02 spread alg refreshToken iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1881",
		label: "ext jwt03 none decodeClaims use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1882",
		label: "ext jwt03 none decodeClaims module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1883",
		label: "ext jwt03 none decodeClaims export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1884",
		label: "ext jwt03 none decodeClaims async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport async function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1885",
		label: "ext jwt03 none decodeClaims iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1886",
		label: "ext jwt04 ignoreExp decodeClaims use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1887",
		label: "ext jwt04 ignoreExp decodeClaims module banner",
		source:
			'/** module */\nimport { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1888",
		label: "ext jwt04 ignoreExp decodeClaims export const",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1889",
		label: "ext jwt04 ignoreExp decodeClaims async export",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport async function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1890",
		label: "ext jwt04 ignoreExp decodeClaims iife suffix",
		source:
			'import { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1891",
		label: "ext jwt06 noTimestamp decodeClaims use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nexport function decodeClaims(p: object, s: string) {\n  return sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1892",
		label: "ext jwt06 noTimestamp decodeClaims module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1893",
		label: "ext jwt06 noTimestamp decodeClaims export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1894",
		label: "ext jwt06 noTimestamp decodeClaims async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport async function decodeClaims(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1895",
		label: "ext jwt06 noTimestamp decodeClaims iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nexport function decodeClaims(p: object, s: string) {\n  return sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1896",
		label: "ext jwt02 spread alg decodeClaims use strict",
		source:
			'"use strict";\nimport { verify, sign } from "jsonwebtoken";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1897",
		label: "ext jwt02 spread alg decodeClaims module banner",
		source:
			"/** module */\nimport { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1898",
		label: "ext jwt02 spread alg decodeClaims export const",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1899",
		label: "ext jwt02 spread alg decodeClaims async export",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport async function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1900",
		label: "ext jwt02 spread alg decodeClaims iife suffix",
		source:
			"import { verify, sign } from \"jsonwebtoken\";\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1901",
		label: "ext jwt03 none authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1902",
		label: "ext jwt03 none authenticate module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1903",
		label: "ext jwt03 none authenticate export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1904",
		label: "ext jwt03 none authenticate async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1905",
		label: "ext jwt03 none authenticate iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1906",
		label: "ext jwt04 ignoreExp authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1907",
		label: "ext jwt04 ignoreExp authenticate module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1908",
		label: "ext jwt04 ignoreExp authenticate export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1909",
		label: "ext jwt04 ignoreExp authenticate async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1910",
		label: "ext jwt04 ignoreExp authenticate iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1911",
		label: "ext jwt06 noTimestamp authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1912",
		label: "ext jwt06 noTimestamp authenticate module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1913",
		label: "ext jwt06 noTimestamp authenticate export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1914",
		label: "ext jwt06 noTimestamp authenticate async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1915",
		label: "ext jwt06 noTimestamp authenticate iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1916",
		label: "ext jwt02 spread alg authenticate use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1917",
		label: "ext jwt02 spread alg authenticate module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1918",
		label: "ext jwt02 spread alg authenticate export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1919",
		label: "ext jwt02 spread alg authenticate async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1920",
		label: "ext jwt02 spread alg authenticate iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1921",
		label: "ext jwt03 none handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1922",
		label: "ext jwt03 none handleAuth module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1923",
		label: "ext jwt03 none handleAuth export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1924",
		label: "ext jwt03 none handleAuth async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1925",
		label: "ext jwt03 none handleAuth iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1926",
		label: "ext jwt04 ignoreExp handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1927",
		label: "ext jwt04 ignoreExp handleAuth module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1928",
		label: "ext jwt04 ignoreExp handleAuth export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1929",
		label: "ext jwt04 ignoreExp handleAuth async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1930",
		label: "ext jwt04 ignoreExp handleAuth iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1931",
		label: "ext jwt06 noTimestamp handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1932",
		label: "ext jwt06 noTimestamp handleAuth module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1933",
		label: "ext jwt06 noTimestamp handleAuth export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1934",
		label: "ext jwt06 noTimestamp handleAuth async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1935",
		label: "ext jwt06 noTimestamp handleAuth iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1936",
		label: "ext jwt02 spread alg handleAuth use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1937",
		label: "ext jwt02 spread alg handleAuth module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1938",
		label: "ext jwt02 spread alg handleAuth export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1939",
		label: "ext jwt02 spread alg handleAuth async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1940",
		label: "ext jwt02 spread alg handleAuth iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1941",
		label: "ext jwt03 none verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1942",
		label: "ext jwt03 none verifyRequest module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1943",
		label: "ext jwt03 none verifyRequest export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1944",
		label: "ext jwt03 none verifyRequest async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1945",
		label: "ext jwt03 none verifyRequest iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1946",
		label: "ext jwt04 ignoreExp verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1947",
		label: "ext jwt04 ignoreExp verifyRequest module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1948",
		label: "ext jwt04 ignoreExp verifyRequest export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1949",
		label: "ext jwt04 ignoreExp verifyRequest async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1950",
		label: "ext jwt04 ignoreExp verifyRequest iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1951",
		label: "ext jwt06 noTimestamp verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1952",
		label: "ext jwt06 noTimestamp verifyRequest module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1953",
		label: "ext jwt06 noTimestamp verifyRequest export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1954",
		label: "ext jwt06 noTimestamp verifyRequest async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1955",
		label: "ext jwt06 noTimestamp verifyRequest iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1956",
		label: "ext jwt02 spread alg verifyRequest use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1957",
		label: "ext jwt02 spread alg verifyRequest module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1958",
		label: "ext jwt02 spread alg verifyRequest export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1959",
		label: "ext jwt02 spread alg verifyRequest async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1960",
		label: "ext jwt02 spread alg verifyRequest iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1961",
		label: "ext jwt03 none issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1962",
		label: "ext jwt03 none issueCredentials module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1963",
		label: "ext jwt03 none issueCredentials export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1964",
		label: "ext jwt03 none issueCredentials async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1965",
		label: "ext jwt03 none issueCredentials iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1966",
		label: "ext jwt04 ignoreExp issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1967",
		label: "ext jwt04 ignoreExp issueCredentials module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1968",
		label: "ext jwt04 ignoreExp issueCredentials export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1969",
		label: "ext jwt04 ignoreExp issueCredentials async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1970",
		label: "ext jwt04 ignoreExp issueCredentials iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1971",
		label: "ext jwt06 noTimestamp issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1972",
		label: "ext jwt06 noTimestamp issueCredentials module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1973",
		label: "ext jwt06 noTimestamp issueCredentials export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1974",
		label: "ext jwt06 noTimestamp issueCredentials async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1975",
		label: "ext jwt06 noTimestamp issueCredentials iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1976",
		label: "ext jwt02 spread alg issueCredentials use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1977",
		label: "ext jwt02 spread alg issueCredentials module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1978",
		label: "ext jwt02 spread alg issueCredentials export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1979",
		label: "ext jwt02 spread alg issueCredentials async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1980",
		label: "ext jwt02 spread alg issueCredentials iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "1981",
		label: "ext jwt03 none parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1982",
		label: "ext jwt03 none parseBearer module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1983",
		label: "ext jwt03 none parseBearer export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1984",
		label: "ext jwt03 none parseBearer async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1985",
		label: "ext jwt03 none parseBearer iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "1986",
		label: "ext jwt04 ignoreExp parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1987",
		label: "ext jwt04 ignoreExp parseBearer module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1988",
		label: "ext jwt04 ignoreExp parseBearer export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1989",
		label: "ext jwt04 ignoreExp parseBearer async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1990",
		label: "ext jwt04 ignoreExp parseBearer iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "1991",
		label: "ext jwt06 noTimestamp parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1992",
		label: "ext jwt06 noTimestamp parseBearer module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1993",
		label: "ext jwt06 noTimestamp parseBearer export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1994",
		label: "ext jwt06 noTimestamp parseBearer async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1995",
		label: "ext jwt06 noTimestamp parseBearer iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "1996",
		label: "ext jwt02 spread alg parseBearer use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "1997",
		label: "ext jwt02 spread alg parseBearer module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "1998",
		label: "ext jwt02 spread alg parseBearer export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "1999",
		label: "ext jwt02 spread alg parseBearer async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2000",
		label: "ext jwt02 spread alg parseBearer iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "2001",
		label: "ext jwt03 none validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2002",
		label: "ext jwt03 none validateSession module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2003",
		label: "ext jwt03 none validateSession export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2004",
		label: "ext jwt03 none validateSession async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2005",
		label: "ext jwt03 none validateSession iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2006",
		label: "ext jwt04 ignoreExp validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2007",
		label: "ext jwt04 ignoreExp validateSession module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2008",
		label: "ext jwt04 ignoreExp validateSession export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2009",
		label: "ext jwt04 ignoreExp validateSession async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2010",
		label: "ext jwt04 ignoreExp validateSession iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2011",
		label: "ext jwt06 noTimestamp validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2012",
		label: "ext jwt06 noTimestamp validateSession module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2013",
		label: "ext jwt06 noTimestamp validateSession export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2014",
		label: "ext jwt06 noTimestamp validateSession async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2015",
		label: "ext jwt06 noTimestamp validateSession iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2016",
		label: "ext jwt02 spread alg validateSession use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "2017",
		label: "ext jwt02 spread alg validateSession module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2018",
		label: "ext jwt02 spread alg validateSession export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "2019",
		label: "ext jwt02 spread alg validateSession async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2020",
		label: "ext jwt02 spread alg validateSession iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "2021",
		label: "ext jwt03 none loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2022",
		label: "ext jwt03 none loginUser module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2023",
		label: "ext jwt03 none loginUser export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2024",
		label: "ext jwt03 none loginUser async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2025",
		label: "ext jwt03 none loginUser iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2026",
		label: "ext jwt04 ignoreExp loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2027",
		label: "ext jwt04 ignoreExp loginUser module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2028",
		label: "ext jwt04 ignoreExp loginUser export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2029",
		label: "ext jwt04 ignoreExp loginUser async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2030",
		label: "ext jwt04 ignoreExp loginUser iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2031",
		label: "ext jwt06 noTimestamp loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2032",
		label: "ext jwt06 noTimestamp loginUser module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2033",
		label: "ext jwt06 noTimestamp loginUser export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2034",
		label: "ext jwt06 noTimestamp loginUser async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2035",
		label: "ext jwt06 noTimestamp loginUser iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2036",
		label: "ext jwt02 spread alg loginUser use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "2037",
		label: "ext jwt02 spread alg loginUser module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2038",
		label: "ext jwt02 spread alg loginUser export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "2039",
		label: "ext jwt02 spread alg loginUser async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2040",
		label: "ext jwt02 spread alg loginUser iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "2041",
		label: "ext jwt03 none middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2042",
		label: "ext jwt03 none middleware module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2043",
		label: "ext jwt03 none middleware export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2044",
		label: "ext jwt03 none middleware async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2045",
		label: "ext jwt03 none middleware iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2046",
		label: "ext jwt04 ignoreExp middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2047",
		label: "ext jwt04 ignoreExp middleware module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2048",
		label: "ext jwt04 ignoreExp middleware export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2049",
		label: "ext jwt04 ignoreExp middleware async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2050",
		label: "ext jwt04 ignoreExp middleware iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2051",
		label: "ext jwt06 noTimestamp middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2052",
		label: "ext jwt06 noTimestamp middleware module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2053",
		label: "ext jwt06 noTimestamp middleware export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2054",
		label: "ext jwt06 noTimestamp middleware async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2055",
		label: "ext jwt06 noTimestamp middleware iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2056",
		label: "ext jwt02 spread alg middleware use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "2057",
		label: "ext jwt02 spread alg middleware module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2058",
		label: "ext jwt02 spread alg middleware export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "2059",
		label: "ext jwt02 spread alg middleware async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2060",
		label: "ext jwt02 spread alg middleware iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "2061",
		label: "ext jwt03 none refreshToken use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2062",
		label: "ext jwt03 none refreshToken module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2063",
		label: "ext jwt03 none refreshToken export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2064",
		label: "ext jwt03 none refreshToken async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2065",
		label: "ext jwt03 none refreshToken iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2066",
		label: "ext jwt04 ignoreExp refreshToken use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2067",
		label: "ext jwt04 ignoreExp refreshToken module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2068",
		label: "ext jwt04 ignoreExp refreshToken export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2069",
		label: "ext jwt04 ignoreExp refreshToken async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2070",
		label: "ext jwt04 ignoreExp refreshToken iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2071",
		label: "ext jwt06 noTimestamp refreshToken use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2072",
		label: "ext jwt06 noTimestamp refreshToken module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2073",
		label: "ext jwt06 noTimestamp refreshToken export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2074",
		label: "ext jwt06 noTimestamp refreshToken async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2075",
		label: "ext jwt06 noTimestamp refreshToken iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function refreshToken(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2076",
		label: "ext jwt02 spread alg refreshToken use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "2077",
		label: "ext jwt02 spread alg refreshToken module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2078",
		label: "ext jwt02 spread alg refreshToken export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "2079",
		label: "ext jwt02 spread alg refreshToken async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2080",
		label: "ext jwt02 spread alg refreshToken iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function refreshToken(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
	{
		id: "2081",
		label: "ext jwt03 none decodeClaims use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2082",
		label: "ext jwt03 none decodeClaims module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2083",
		label: "ext jwt03 none decodeClaims export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2084",
		label: "ext jwt03 none decodeClaims async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2085",
		label: "ext jwt03 none decodeClaims iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = {};\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-03": 1,
		},
	},
	{
		id: "2086",
		label: "ext jwt04 ignoreExp decodeClaims use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2087",
		label: "ext jwt04 ignoreExp decodeClaims module banner",
		source:
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2088",
		label: "ext jwt04 ignoreExp decodeClaims export const",
		source:
			'const jwt = require("jsonwebtoken");\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2089",
		label: "ext jwt04 ignoreExp decodeClaims async export",
		source:
			'const jwt = require("jsonwebtoken");\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2090",
		label: "ext jwt04 ignoreExp decodeClaims iife suffix",
		source:
			'const jwt = require("jsonwebtoken");\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\n;(function noop() { return 0; })();\n',
		expect: {
			"CS-JWT-04": 1,
			"CS-JWT-02": 1,
		},
	},
	{
		id: "2091",
		label: "ext jwt06 noTimestamp decodeClaims use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2092",
		label: "ext jwt06 noTimestamp decodeClaims module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2093",
		label: "ext jwt06 noTimestamp decodeClaims export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2094",
		label: "ext jwt06 noTimestamp decodeClaims async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport async function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2095",
		label: "ext jwt06 noTimestamp decodeClaims iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nexport function decodeClaims(p: object, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {
			"CS-JWT-05": 1,
			"CS-JWT-06": 1,
		},
	},
	{
		id: "2096",
		label: "ext jwt02 spread alg decodeClaims use strict",
		source:
			'"use strict";\nconst jwt = require("jsonwebtoken");\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: [\'HS256\'] });\n}\n',
		expect: {},
	},
	{
		id: "2097",
		label: "ext jwt02 spread alg decodeClaims module banner",
		source:
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2098",
		label: "ext jwt02 spread alg decodeClaims export const",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		expect: {},
	},
	{
		id: "2099",
		label: "ext jwt02 spread alg decodeClaims async export",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport async function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n",
		expect: {},
	},
	{
		id: "2100",
		label: "ext jwt02 spread alg decodeClaims iife suffix",
		source:
			"const jwt = require(\"jsonwebtoken\");\nconst opts = { clockTolerance: 1 };\nexport function decodeClaims(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts, algorithms: ['HS256'] });\n}\n\n;(function noop() { return 0; })();\n",
		expect: {},
	},
];

describe("CS-EXH JWT exhaustive edge cases", () => {
	it.each(cases)("$id $label", async (c) => {
		const result = await scanSource(`case-${c.id}.ts`, c.source);
		const expected = c.expect;
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
