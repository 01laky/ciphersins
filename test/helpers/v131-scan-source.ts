/** @deprecated Import from ./scan-source.js — kept for legacy generated paths during migration. */
export {
	assertFindingAt,
	assertOnlyRules,
	byRule,
	countRule,
	scanSource,
	scanSources,
	type ScanSourceOptions,
} from "./scan-source.js";
